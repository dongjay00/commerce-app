# 프론트엔드 상점 + Playwright E2E Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 계획 1~4가 완성한 API 위에 FSD 상점 화면을 짓고, 스펙 §13의 기능 성공 기준을 브라우저 E2E 7개로 증명한다.

**Architecture:** 프론트에는 헥사고날을 적용하지 않고 FSD를 쓴다(스펙 §3.2) — 레이어가 아래로만 의존하고(`shared ← entities ← features ← widgets ← views`), 같은 dependency-cruiser 설정이 백엔드와 함께 강제한다. 조회는 `app/`의 RSC가 `src/server/api-client`로 직접 하고(스펙 §8.7), 변경은 클라이언트 → Route Handler → Nest로 간다. 테스트 seam은 포트가 아니라 **네트워크**이고 MSW가 그 자리이며, 핸들러는 `@commerce/contracts`의 Zod 스키마로 요청과 응답을 검증한다(스펙 §9.9).

**Tech Stack:** Next 16 (App Router) · React 19 · TypeScript · ts-rest 3.52.1 + Zod 3 · iron-session 9 · MSW 2 · Testing Library · Vitest 3.2.7 · Playwright · Biome

**Spec:** `docs/superpowers/specs/2026-09-01-commerce-ordering-design.md`

**선행 계획:**
- `docs/superpowers/plans/2026-09-02-foundation-skeleton.md` (계획 1 — 골격, BFF 세션·토큰, MSW 골격)
- `docs/superpowers/plans/2026-09-02-identity-customer.md` (계획 2 — 인증 API, 주소록 API)
- `docs/superpowers/plans/2026-09-03-catalog-inventory.md` (계획 3 — 상품·재고 API)
- `docs/superpowers/plans/2026-09-03-payment-ordering-saga.md` (계획 4 — 장바구니·주문 API, 사가)

---

## Global Constraints

스펙의 프로젝트 전역 요구사항이다. **모든 태스크의 요구사항에 암묵적으로 포함된다.**

- **FSD 레이어는 아래로만 의존한다** — `shared ← entities ← features ← widgets ← views`. `pnpm arch:check`의 `fsd-shared-is-a-leaf`·`fsd-entities-layer-direction`·`fsd-features-layer-direction`·`fsd-widgets-layer-direction`가 강제한다.
- **슬라이스 간 참조는 `index.ts`로만 한다** — `fsd-no-cross-slice-internals`. 같은 레이어의 다른 슬라이스 내부(`ui/`, `model/`)를 직접 import할 수 없다.
- **FSD 레이어는 `src/server/`를 import할 수 없다** — `no-server-code-in-fsd`. BFF 전용 코드(토큰·세션)가 화면 레이어로 새면 안 된다. **이 규칙이 이 계획의 구조를 결정한다** — 아래 "편차 1" 참조.
- **`apps/web`은 `apps/api`를 import하지 않는다** — `web-must-not-import-api`. 공유는 `@commerce/contracts`로만.
- **BFF는 계산하지 않는다** (스펙 §8.1). 전달·병합·형태 변환만 한다. 주문 총액이나 취소 가능 여부를 BFF가 판단하는 순간 같은 규칙이 두 곳에 생긴다 — **화면에 필요하면 API가 계산된 값을 내려준다.**
- **브라우저는 액세스 토큰을 보지 않는다** (스펙 §8.5). 세션 접근은 `src/server/` 안에서만 하고, 쿠키는 `HttpOnly; Secure; SameSite=Lax`다.
- **계약 패키지에는 DTO만 넣는다** (스펙 §8.2). `Order` 엔티티나 `Money` VO를 넣으면 프론트가 도메인 타입에 묶인다.
- **목 라이브러리를 쓰지 않는다** (스펙 §9.1). `vi.mock`, `vi.spyOn` 금지. 네트워크는 MSW로, 나머지는 손으로 쓴 fake로.
- **MSW 핸들러는 계약 스키마로 요청과 응답을 검증한다** (스펙 §9.9). 백엔드 계약이 바뀌면 프론트 목이 즉시 깨져야 한다.
- **TDD는 `model` 세그먼트에만 적용한다** (스펙 §9.9). UI 컴포넌트는 test-after — 레이아웃과 픽셀은 테스트를 먼저 쓸 수 없고, 억지로 하면 구현을 베낀 무의미한 테스트가 나온다.
- **E2E는 스펙 §9.10이 나열한 일곱 시나리오 영역만 덮는다** — 회원가입/로그인, 상품 조회, 장바구니, 주문 성공, 결제 거절 보상, 주문 취소 환불, 재고 부족. 늘리면 유지비만 커지고 새로 잡는 버그가 없다.
- **버전을 고정한다.** 새 의존성은 `pnpm add pkg@^X.Y.Z` 형태로 정확한 범위를 쓴다.
- **`pnpm verify`가 exit 0이어야 커밋한다.** `&&`로 `verify`와 `git commit`을 한 줄에 잇지 말 것 — 계획 3·4에서 실패한 verify 뒤에 커밋이 나가는 사고가 세 번 났다.
- **Biome 포맷은 `pnpm biome check --write <path>`로 맞춘다.**

---

## 이 계획의 범위

계획 4가 남긴 것 전부다 — 스펙 §9.9(프론트 테스트), §9.10(E2E), §10.7(`apps/web` 구조), 그리고 §13의 기능 성공 기준을 브라우저에서 증명하는 일.

**계획 4가 API 레벨 E2E로 이미 증명한 것을 다시 증명하지 않는다.** 사가의 세 보상 경로는 `apps/api/test/saga/`가 실제 Postgres + outbox 릴레이 + 이벤트 체인을 관통해 검증했다. 브라우저 E2E는 **사용자가 그 경로를 실제로 밟을 수 있는가**를 본다 — 화면이 있고, 버튼이 동작하고, 결과가 보이는가. 두 스위트는 다른 것을 본다.

**만들지 않는 것**

| 안 만드는 것 | 이유 |
|---|---|
| 주문 목록 화면 (`GET /orders`) | 주문 후 상세로 바로 가고 취소도 상세에서 한다. 목록 API는 계획 4가 만들었고 화면은 호출자가 생길 때 만든다 |
| 주소록 전체 CRUD 화면 | 결제 화면에 "배송지 선택 + 없으면 추가"만 둔다. 수정·삭제·기본 배송지 변경 UI는 백로그 |
| Tailwind 등 스타일 시스템 | 전역 CSS 하나와 시맨틱 HTML로 간다. 이 프로젝트의 학습 목표(FSD 경계, 계약 기반 테스트)에 스타일 시스템은 기여하지 않고, E2E는 `getByRole`/`getByText`로 찾으므로 클래스명에 의존하지 않는다 |
| 상품 검색·페이징 UI | `searchProductsQuerySchema`의 `keyword`/`limit`/`offset`은 API에 있다. 화면은 전체 목록만 보여준다 |

---

## 이 계획이 감수하는 편차

### 편차 1 — `views`가 데이터를 가져오지 않는다. `app/`이 가져와 props로 넘긴다

스펙 §9.9는 "`views`(RSC) — E2E로 커버, 미적용"이라고 적었다. RSC를 단위 테스트하기 어렵다는 것이 근거다.

그런데 **`no-server-code-in-fsd` 규칙이 `views`가 `src/server/api-client`를 import하는 것을 금지한다.** 규칙은 계획 1이 만들었고 스펙 §11.2가 요구한 것이다. 그러므로 `views`는 애초에 페치할 수 없다.

결과적으로 구조가 이렇게 갈린다.

```
app/(shop)/products/[id]/page.tsx     RSC. api-client로 페치하고 props로 넘긴다. 로직 없음
  └─ views/product-detail             순수 컴포넌트. props를 받아 그린다
       └─ widgets/product-grid, features/cart-items, entities/product
```

**이것은 스펙보다 나은 결과다.** `views`가 순수 컴포넌트가 되므로 Testing Library로 테스트할 수 있고, E2E가 덮어야 하는 것은 `app/`의 얇은 RSC(페치 3줄)만 남는다. 스펙 §9.9가 "RSC는 페치와 전달만, 로직은 전부 아래 레이어로"라고 적은 그 원칙을 규칙이 구조적으로 강제하는 셈이다.

**대가**: `app/` 아래의 페치 코드에는 단위 테스트가 없다. 그 코드가 3줄을 넘기 시작하면 로직이 새고 있다는 신호이므로, 각 `page.tsx`를 짧게 유지하는 것이 이 편차의 유일한 규율이다.

### 편차 2 — PG 시나리오를 바꾸는 테스트 전용 엔드포인트를 API에 추가한다

스펙 §9.10의 예시가 `api.fakePg.setScenario('DECLINE_NEXT')`를 쓴다. `FakePgAdapter.scenario`는 Nest 프로세스 안의 상태이고, 브라우저 E2E는 HTTP로만 말한다 — 바꿀 방법이 없다.

`POST /testing/pg-scenario`를 추가하되 **`ENABLE_TEST_ENDPOINTS === 'true'`일 때만 등록한다.** 기본값으로는 컨트롤러가 아예 존재하지 않는 것을 `app.module.spec.ts`가 단언한다.

버린 대안: **매직 금액**(999원이면 거절). 프로덕션 경로의 입력값에 테스트용 의미를 심으면 실서비스에서 그 금액을 결제하는 고객이 거절당한다 — 계획 4의 `fake-pg.adapter.ts` 주석이 같은 이유로 이미 기각했다.

**남는 위험**: 운영 환경에 `ENABLE_TEST_ENDPOINTS=true`가 실수로 켜지면 누구나 결제 시나리오를 바꿀 수 있다. `.env.example`에 그 변수를 **넣지 않고**, Playwright의 `webServer`가 자기 프로세스에만 주입한다. 그리고 그 사실을 컨트롤러 주석과 태스크 15의 이월에 적는다.

### 편차 3 — E2E를 `pnpm verify`에 넣지 않는다

`verify`는 `lint → arch:check → typecheck → build → test:coverage`이고 현재 약 3분이다. E2E는 두 서버와 Postgres를 띄우고 브라우저를 돌린다 — `verify`에 넣으면 커밋마다 그 비용을 치른다.

`pnpm test:e2e`를 따로 둔다. **대가는 명확하다: E2E가 기본 게이트에 없으므로 깨진 채로 커밋될 수 있다.** 계획 4의 사가 E2E는 `test:int`에 있어 `verify`가 덮지만, 브라우저 E2E는 그렇지 않다. CI가 붙는 시점에 별도 잡으로 넣는 것이 정답이고, CI는 아직 없다(계획 1의 이월).

### 편차 4 — 클라이언트 상태 관리 라이브러리를 쓰지 않는다

장바구니 담기, 주문, 취소는 전부 "요청 보내고 → 결과에 따라 화면 갱신"이다. React 19의 `useTransition`과 `router.refresh()`로 충분하다. TanStack Query 같은 것을 넣으면 캐시 무효화 정책이라는 새 개념이 생기고, 이 프로젝트가 가르치려는 것(레이어 경계, 계약 기반 테스트)과 무관한 복잡도다.

**대가**: 낙관적 업데이트가 없다. 담기 버튼을 누르면 서버 왕복만큼 기다린다. 학습 프로젝트에서는 그 편이 흐름이 눈에 보여 낫다.

---

## 계획 1~4가 남긴 이월 중 이 계획이 처리하는 것

| 이월 | 출처 | 이 계획에서 |
|---|---|---|
| 프론트 MSW 핸들러의 계약 검증 | 스펙 §9.9 | **태스크 1** — 상품·장바구니·주문·재고·주소 핸들러 |
| Playwright 브라우저 E2E 6~8 시나리오 | 스펙 §9.10 | **태스크 12~14** |
| `docs/architecture.svg` 생성 | 계획 3 부록 | **태스크 15** — graphviz가 있으면 만들고, 없으면 없다고 적는다 |
| `inventory.module.ts:2`의 불필요한 biome 억제 주석 | 계획 4 부록 | **태스크 15** — 한 줄 삭제 |
| `PlaceOrderService`의 `inject` 배열을 지키는 장치 | 계획 4 부록 | 처리하지 않는다 — 브라우저 E2E도 사가 E2E와 같은 것을 잡을 뿐 더 싸지 않다. 백로그로 남긴다 |
| N+1을 잡는 장치, 예약 전이 동시성 스위트 | 계획 4 부록 | 처리하지 않는다 — 프론트와 무관하다 |

---

## 계획 1~4 산출물 중 수정하는 것

| 파일 | 무엇을 |
|---|---|
| `apps/web/package.json` | `@playwright/test` 추가, `test:e2e` 스크립트 |
| `package.json` (루트) | `test:e2e` 위임 스크립트 |
| `apps/web/src/shared/api/msw/server.ts` | 새 핸들러 등록 |
| `apps/web/app/layout.tsx` | 전역 스타일 import, `widgets/header` 배치 |
| `apps/web/app/page.tsx` | 상품 목록 RSC로 교체 |
| `apps/api/src/app.module.ts` · `app.module.spec.ts` | 테스트 전용 컨트롤러의 조건부 등록과 그 단언 |
| `apps/api/src/modules/inventory/inventory.module.ts` | 불필요한 biome 억제 주석 삭제 |
| `README.md` | 프론트엔드 절, 실행 방법, E2E 실행 방법 |
| `.gitignore` | `playwright-report/`, `test-results/` |

---

## File Structure

### 이미 있는 것 (계획 1이 만든 BFF 골격)

```
apps/web/src/server/               'server-only'. FSD 밖. BFF 전용
├── session.ts                     암호화 쿠키 → TokenStore
├── token-store.ts                 TokenStore 인터페이스 — BFF의 유일한 seam
├── api-client.ts                  토큰 주입 + 401 시 refresh 1회 재시도
├── auth-actions.ts                signInAction / signOutAction
└── safe-json.ts

apps/web/src/shared/
├── api/contract-client.ts         ts-rest 클라이언트 팩토리
├── api/msw/{server.ts, handlers/{auth,health}.ts}
└── lib/format-money.ts            MoneyDto → "12,000원"
```

### 새로 만드는 것

```
apps/web/
├── app/                                       Next 라우팅. **얇은 껍데기**
│   ├── globals.css                            전역 스타일 하나
│   ├── layout.tsx                             (수정) 스타일 + Header
│   ├── page.tsx                               (수정) 상품 목록 RSC
│   ├── products/[productId]/page.tsx          상품 상세 RSC
│   ├── cart/page.tsx                          장바구니 RSC
│   ├── orders/[orderId]/page.tsx              주문 상세 RSC
│   ├── sign-in/page.tsx                       로그인 (클라이언트 폼)
│   └── api/                                   BFF Route Handlers
│       ├── auth/{sign-in,sign-out}/route.ts   (이미 있음)
│       ├── cart/items/route.ts                POST 담기
│       ├── cart/items/[skuId]/route.ts        PUT 수량 변경 / DELETE 빼기
│       ├── addresses/route.ts                 POST 배송지 추가
│       ├── orders/route.ts                    POST 주문
│       └── orders/[orderId]/cancel/route.ts   POST 취소
│
├── src/
│   ├── server/
│   │   ├── cart-actions.ts + spec             addItem / changeQuantity / removeItem
│   │   ├── order-actions.ts + spec            placeOrder / cancelOrder
│   │   └── address-actions.ts + spec          addAddress
│   │
│   ├── views/
│   │   ├── product-list/{ui/ProductListView.tsx, index.ts} + spec
│   │   ├── product-detail/{ui/ProductDetailView.tsx, index.ts} + spec
│   │   ├── cart/{ui/CartView.tsx, index.ts} + spec
│   │   └── order-detail/{ui/OrderDetailView.tsx, index.ts} + spec
│   │
│   ├── widgets/
│   │   ├── header/{ui/Header.tsx, index.ts} + spec
│   │   ├── product-grid/{ui/ProductGrid.tsx, index.ts} + spec
│   │   └── order-summary/{ui/OrderSummary.tsx, index.ts} + spec
│   │
│   ├── features/
│   │   ├── sign-in/{model/use-sign-in.ts, ui/SignInForm.tsx, index.ts} + spec
│   │   ├── sign-out/{model/use-sign-out.ts, ui/SignOutButton.tsx, index.ts} + spec
│   │   ├── cart-items/{model/{use-add-to-cart,use-remove-from-cart}.ts,
│   │   │              ui/{AddToCartButton,RemoveFromCartButton}.tsx, index.ts} + spec
│   │   ├── manage-addresses/{model/use-add-address.ts, ui/AddressPicker.tsx, index.ts} + spec
│   │   ├── place-order/{model/use-place-order.ts, ui/PlaceOrderButton.tsx, index.ts} + spec
│   │   └── cancel-order/{model/use-cancel-order.ts, ui/CancelOrderButton.tsx, index.ts} + spec
│   │
│   ├── entities/
│   │   ├── product/{ui/ProductCard.tsx, index.ts} + spec
│   │   ├── cart/{ui/CartLineRow.tsx, index.ts} + spec
│   │   ├── order/{model/order-status.ts, ui/OrderStatusBadge.tsx, index.ts} + spec
│   │   └── address/{ui/AddressLine.tsx, index.ts} + spec
│   │
│   └── shared/
│       ├── api/msw/handlers/{product,cart,order,stock,address}.ts + spec
│       └── lib/api-error.ts + spec            fetch 응답 → { ok } | { ok: false, code, message }
│
└── e2e/
    ├── playwright.config.ts
    ├── fixtures.ts                            api 픽스처 (시드·PG 시나리오·재고 조회)
    ├── auth.spec.ts                           회원가입/로그인
    ├── browse-and-cart.spec.ts                상품 조회, 장바구니
    └── checkout.spec.ts                       주문 성공, 결제 거절, 취소 환불, 재고 부족

apps/api/src/shared/infrastructure/testing/
├── pg-scenario.controller.ts                  테스트 전용. 편차 2
└── pg-scenario.controller.integration.spec.ts
```

**왜 `entities`가 넷인가.** 화면에 반복해서 나오는 도메인 조각이 그만큼이다 — 상품 카드(목록·상세), 장바구니 줄(장바구니·주문 요약), 주문 상태 배지(주문 상세·향후 목록), 배송지 한 줄(선택기·주문 상세). 각 슬라이스는 **`ui/`만 갖거나 `model/`을 하나 갖는다** — `entities/order/model/order-status.ts`가 유일한 `model`이고, 그것은 상태 코드를 한국어 라벨로 옮기는 순수 함수라 TDD 대상이다.

**왜 `features`가 여섯인가.** 사용자가 일으키는 상태 변경이 그만큼이다 — 로그인·로그아웃·장바구니 줄 조작·배송지·주문·취소. 각 슬라이스는 `model/`에 훅(요청·로딩·에러)과 `ui/`에 버튼/폼을 갖는다. **훅에만 TDD를 적용한다**(스펙 §9.9) — MSW로 네트워크를 세우고 `renderHook`으로 상태 전이를 검증한다.

**`shared/lib/api-error.ts`가 있는 이유.** 일곱 훅이 전부 "fetch 하고, 실패면 `ErrorDto`를 파싱해 `code`로 분기한다"를 반복한다. 그 세 줄을 다섯 번 복제하면 하나만 고쳐지는 날이 온다. 스펙 §8.6이 "프론트가 상태 코드가 아니라 코드로 분기한다"고 적은 규약이 여기 한 곳에 산다.

### Route Handler와 action의 분리

```
app/api/orders/route.ts          접착제 3줄. 테스트 없음
  └─ src/server/order-actions.ts  로직. TokenStore를 주입받아 테스트 가능
```

계획 1의 `auth-actions.ts` / `app/api/auth/sign-in/route.ts`가 세운 형태를 그대로 따른다. Route Handler를 vitest에서 부르려면 Next의 요청 컨텍스트가 필요하고 그걸 흉내내려면 목 라이브러리가 든다(금지) — 그래서 **로직은 전부 action에 두고 Route Handler는 접착제로만 남긴다.** 각 Route Handler는 E2E가 덮는다.

---

## 태스크

각 태스크는 **마지막 직전 단계에 "이 검사가 무엇을 잡는지 증명한다"** 를 갖는다. 방금 쓴 테스트를 통과시킨 코드를 일부러 훼손해 그 테스트가 실제로 실패하는지 확인하고, 되돌린 뒤 다시 통과하는 것까지 본다. 계획 2·3·4에서 이 단계가 **"아무것도 검증하지 않는 테스트" 셋과 프로덕션 결함 둘**을 잡아냈다.

**프루브를 트리에 남기지 않는다.** 되돌린 뒤 `git diff`가 비어 있는 것을 확인하고 커밋한다.

---

### Task 1: MSW 핸들러 확장 — 이후 모든 테스트의 seam

**Files:**
- Create: `apps/web/src/shared/api/msw/handlers/{product,cart,order,address}.ts`
- Create: `apps/web/src/shared/api/msw/handlers/{product,cart,order,address}.spec.ts`
- Create: `apps/web/src/shared/api/msw/fixtures.ts`
- Modify: `apps/web/src/shared/api/msw/server.ts`
- Modify: `apps/web/src/shared/api/msw/handlers/auth.ts` (`/auth/sign-out` 추가)

**Interfaces:**
- Consumes: `@commerce/contracts`의 `productContract`·`cartContract`·`orderContract`·`addressContract` 스키마
- Produces:
  - `productHandlers`, `cartHandlers`, `orderHandlers`, `addressHandlers`
  - `apiBase()` — `process.env['API_BASE_URL'] ?? 'http://localhost:3001'`
  - 픽스처 상수: `PRODUCT_ID`, `SKU_ID`, `ORDER_ID`, `ADDRESS_ID`, `aProductDto()`, `aCartDto()`, `anOrderDto()`, `anAddressDto()`

**이 태스크가 이 계획 전체의 토대다.** 스펙 §9.9: "핸들러는 `@commerce/contracts`의 Zod 스키마로 요청과 응답을 검증한다. 백엔드 계약이 바뀌면 프론트 목이 즉시 깨진다 — 손으로 만든 fake는 조용히 드리프트하지만 이 방식은 구조적으로 불가능하다."

**핸들러가 상태를 갖지 않는다.** 각 핸들러는 고정된 응답을 돌려준다. 상태(담으면 늘어나는 장바구니)를 흉내내면 그 시뮬레이터가 진짜 서버와 조용히 갈라지고, 그 드리프트를 잡을 장치가 없다. 상태가 필요한 검증은 브라우저 E2E가 진짜 서버로 한다. 테스트가 특정 응답을 필요로 하면 `server.use()`로 그 테스트 안에서만 덮어쓴다.

- [ ] **Step 1: 픽스처를 만든다**

`fixtures.ts` — 여러 핸들러와 컴포넌트 테스트가 같은 값을 다시 타이핑하지 않게 모은다.

```ts
import type {
  AddressDto,
  CartDto,
  OrderDto,
  ProductDto,
} from '@commerce/contracts';

/**
 * UUID 리터럴은 유효한 16진수여야 하고 마지막 그룹이 정확히 12자여야 한다 —
 * 백엔드의 값 객체가 형식을 검증한다. 계획 3·4에서 `l`, `ver`, `dup` 때문에
 * 세 번 깨졌으므로 16진수 안에서만 구분한다.
 */
export const PRODUCT_ID = '018f2b1c-4a5d-7e6f-8a9b-0f1a00000001';
export const SKU_ID = '018f2b1c-4a5d-7e6f-8a9b-0f1c00000001';
export const SKU_ID_2 = '018f2b1c-4a5d-7e6f-8a9b-0f1c00000002';
export const ORDER_ID = '018f2b1c-4a5d-7e6f-8a9b-0f1b00000001';
export const ADDRESS_ID = '018f2b1c-4a5d-7e6f-8a9b-0f1e00000001';
export const CART_ID = '018f2b1c-4a5d-7e6f-8a9b-0f1d00000001';

export const KRW = (amount: string) => ({ amount, currency: 'KRW' as const });

export function aProductDto(overrides: Partial<ProductDto> = {}): ProductDto {
  return {
    id: PRODUCT_ID,
    name: '티셔츠',
    status: 'ACTIVE',
    skus: [
      { id: SKU_ID, code: 'RED-M', price: KRW('12000') },
      { id: SKU_ID_2, code: 'RED-L', price: KRW('13000') },
    ],
    ...overrides,
  };
}

export function aCartDto(overrides: Partial<CartDto> = {}): CartDto {
  return {
    cartId: CART_ID,
    lines: [
      {
        skuId: SKU_ID,
        nameSnapshot: '티셔츠 RED-M',
        unitPrice: KRW('12000'),
        quantity: 2,
        subtotal: KRW('24000'),
      },
    ],
    total: KRW('24000'),
    unavailableSkuIds: [],
    ...overrides,
  };
}

export function anOrderDto(overrides: Partial<OrderDto> = {}): OrderDto {
  return {
    id: ORDER_ID,
    status: 'PAID',
    total: KRW('24000'),
    placedAt: '2026-03-01T00:00:00.000Z',
    shippingAddress: {
      recipient: '홍길동',
      phone: '010-1234-5678',
      zip: '06236',
      line1: '서울시 강남구 테헤란로 1',
      line2: null,
    },
    lines: [
      {
        skuId: SKU_ID,
        nameSnapshot: '티셔츠 RED-M',
        unitPrice: KRW('12000'),
        quantity: 2,
        subtotal: KRW('24000'),
      },
    ],
    ...overrides,
  };
}

export function anAddressDto(overrides: Partial<AddressDto> = {}): AddressDto {
  return {
    id: ADDRESS_ID,
    label: '집',
    recipient: '홍길동',
    phone: '010-1234-5678',
    zip: '06236',
    line1: '서울시 강남구 테헤란로 1',
    isDefault: true,
    ...overrides,
  };
}
```

- [ ] **Step 2: 핸들러를 쓴다**

`product.ts`:

```ts
import { productContract } from '@commerce/contracts';
import { HttpResponse, http } from 'msw';
import { aProductDto, PRODUCT_ID } from '../fixtures';

const BASE = process.env['API_BASE_URL'] ?? 'http://localhost:3001';

/**
 * **응답을 계약 스키마로 파싱한 뒤 돌려준다.** 픽스처가 계약에서 벗어나면 목이
 * 즉시 깨진다 — 손으로 만든 fake는 조용히 드리프트하지만 이 방식은 구조적으로
 * 불가능하다(스펙 §9.9).
 */
export const productHandlers = [
  http.get(`${BASE}/products`, () =>
    HttpResponse.json(productContract.search.responses[200].parse({ products: [aProductDto()] })),
  ),
  http.get(`${BASE}/products/${PRODUCT_ID}`, () =>
    HttpResponse.json(productContract.get.responses[200].parse(aProductDto())),
  ),
];
```

`cart.ts` — 조회는 응답을, 변경은 **요청 본문을** 계약으로 파싱한다.

```ts
import { addCartItemBodySchema, cartContract, changeCartItemBodySchema } from '@commerce/contracts';
import { HttpResponse, http } from 'msw';
import { aCartDto } from '../fixtures';

const BASE = process.env['API_BASE_URL'] ?? 'http://localhost:3001';

export const cartHandlers = [
  http.get(`${BASE}/cart`, () =>
    HttpResponse.json(cartContract.get.responses[200].parse(aCartDto())),
  ),
  http.post(`${BASE}/cart/items`, async ({ request }) => {
    // 요청이 계약을 벗어나면 여기서 던진다 — 훅이 잘못된 본문을 보내는 회귀를 잡는다.
    addCartItemBodySchema.parse(await request.json());
    return new HttpResponse(null, { status: 204 });
  }),
  http.put(`${BASE}/cart/items/:skuId`, async ({ request }) => {
    changeCartItemBodySchema.parse(await request.json());
    return new HttpResponse(null, { status: 204 });
  }),
  http.delete(`${BASE}/cart/items/:skuId`, () => new HttpResponse(null, { status: 204 })),
];
```

`order.ts`:

```ts
import { orderContract, placeOrderBodySchema } from '@commerce/contracts';
import { HttpResponse, http } from 'msw';
import { anOrderDto, ORDER_ID } from '../fixtures';

const BASE = process.env['API_BASE_URL'] ?? 'http://localhost:3001';

export const orderHandlers = [
  http.post(`${BASE}/orders`, async ({ request }) => {
    placeOrderBodySchema.parse(await request.json());
    return HttpResponse.json(
      orderContract.place.responses[201].parse({ orderId: ORDER_ID, status: 'PAID' }),
      { status: 201 },
    );
  }),
  http.get(`${BASE}/orders/${ORDER_ID}`, () =>
    HttpResponse.json(orderContract.get.responses[200].parse(anOrderDto())),
  ),
  http.post(`${BASE}/orders/${ORDER_ID}/cancel`, () =>
    HttpResponse.json(orderContract.cancel.responses[200].parse({ status: 'REFUND_PENDING' })),
  ),
];
```

`address.ts`:

```ts
import { addressBodySchema, addressContract } from '@commerce/contracts';
import { HttpResponse, http } from 'msw';
import { anAddressDto } from '../fixtures';

const BASE = process.env['API_BASE_URL'] ?? 'http://localhost:3001';

// 라우트 이름은 `addressContract`의 것이다: list / add / update / remove / setDefault.
export const addressHandlers = [
  http.get(`${BASE}/addresses`, () =>
    HttpResponse.json(addressContract.list.responses[200].parse({ addresses: [anAddressDto()] })),
  ),
  http.post(`${BASE}/addresses`, async ({ request }) => {
    addressBodySchema.parse(await request.json());
    return HttpResponse.json(addressContract.add.responses[201].parse(anAddressDto()), {
      status: 201,
    });
  }),
];
```

`auth.ts`에 로그아웃을 더한다. `authContract.signOut`은 `refreshBodySchema` 본문을 받고 204를 돌려준다(이미 없는 토큰이어도 멱등).

```ts
import { refreshBodySchema, signInBodySchema, signUpBodySchema } from '@commerce/contracts';

  http.post(`${BASE}/auth/sign-out`, async ({ request }) => {
    refreshBodySchema.parse(await request.json());
    return new HttpResponse(null, { status: 204 });
  }),
```

`server.ts`:

```ts
export const server = setupServer(
  ...healthHandlers,
  ...authHandlers,
  ...productHandlers,
  ...cartHandlers,
  ...orderHandlers,
  ...addressHandlers,
);
```

- [ ] **Step 3: 핸들러 spec을 쓴다**

`product.spec.ts` — 계획 1의 `health.spec.ts`가 세운 형태를 따른다.

```ts
import { productContract } from '@commerce/contracts';
import { describe, expect, it } from 'vitest';
import { PRODUCT_ID, SKU_ID } from '../fixtures';

const BASE = process.env['API_BASE_URL'] ?? 'http://localhost:3001';

describe('product MSW 핸들러', () => {
  it('목록 응답이 계약 스키마를 만족한다', async () => {
    const body = await (await fetch(`${BASE}/products`)).json();
    expect(() => productContract.search.responses[200].parse(body)).not.toThrow();
  });

  it('상세 응답이 계약 스키마를 만족하고 SKU를 담고 있다', async () => {
    const body = await (await fetch(`${BASE}/products/${PRODUCT_ID}`)).json();
    expect(() => productContract.get.responses[200].parse(body)).not.toThrow();
    expect(body.skus.map((sku: { id: string }) => sku.id)).toContain(SKU_ID);
  });
});
```

`cart.spec.ts`는 **요청 검증이 실제로 도는지**를 확인한다 — 이것이 이 스위트의 핵심이다.

```ts
describe('cart MSW 핸들러', () => {
  it('조회 응답이 계약 스키마를 만족한다', async () => {
    const body = await (await fetch(`${BASE}/cart`)).json();
    expect(() => cartContract.get.responses[200].parse(body)).not.toThrow();
  });

  it('담기 요청이 계약을 만족하면 204다', async () => {
    const response = await fetch(`${BASE}/cart/items`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ skuId: SKU_ID, quantity: 2 }),
    });
    expect(response.status).toBe(204);
  });

  it('계약을 벗어난 담기 요청은 핸들러가 던진다', async () => {
    // MSW의 onUnhandledRequest는 'error'지만 핸들러 안의 예외는 500으로 나온다.
    // 이 단언이 "요청 검증이 실제로 돈다"를 증명한다 — 없으면 handlers가 본문을
    // 무시해도 아무도 모른다.
    const response = await fetch(`${BASE}/cart/items`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ skuId: 'not-a-uuid', quantity: 0 }),
    });
    expect(response.status).toBeGreaterThanOrEqual(500);
  });
});
```

`order.spec.ts`와 `address.spec.ts`도 같은 셋(조회 응답 계약 만족 / 정상 요청 통과 / 계약 위반 요청이 던짐)을 덮는다.

- [ ] **Step 4: 통과를 확인한다**

Run: `pnpm vitest run apps/web/src/shared/api/msw`
Expected: PASS

- [ ] **Step 5: 이 검사가 무엇을 잡는지 증명한다**

**(a) 응답 검증이 실제로 도는가**
`product.ts`의 `aProductDto()`에 `{ status: 'SOLD_OUT' as never }`를 넘긴다(계약의 열거값 밖).
Expected: FAIL — 핸들러가 파싱에서 던지고 `'목록 응답이 계약 스키마를 만족한다'`가 500을 받아 실패한다. **이것이 §9.9가 말한 "계약이 바뀌면 프론트 목이 즉시 깨진다"의 실제 동작이다.**
되돌린다.

**(b) 요청 검증이 실제로 도는가**
`cart.ts`의 `addCartItemBodySchema.parse(await request.json());`를 지운다.
Expected: FAIL — `'계약을 벗어난 담기 요청은 핸들러가 던진다'`가 204를 받아 실패한다.
되돌린다.

**(c) 파싱 결과를 돌려주는가, 원본을 돌려주는가**
`product.ts`의 `HttpResponse.json(productContract.search.responses[200].parse({...}))`를 `HttpResponse.json({ products: [aProductDto()] })`로 바꾼다(파싱은 하지 않고 원본을 반환).
Expected: **통과한다.** 픽스처가 계약을 만족하므로 결과가 같다. 관측 결과를 보고서에 적는다 — `.parse()`의 값은 반환값이 아니라 **던진다는 사실**에 있고, `.strict()` 스키마라면 여분 필드를 떨어뜨리는 부수 효과도 있다는 것을 기록한다.
되돌린다.

- [ ] **Step 6: 전체 검증과 커밋**

Run: `pnpm verify`
Expected: exit 0.

```bash
git add apps/web/src/shared/api
git commit -m "test(web): 상품·장바구니·주문·주소 MSW 핸들러를 계약으로 검증한다"
```

---

### Task 2: `shared/lib/api-error.ts` — 에러 코드 분기의 단일 지점

**Files:**
- Create: `apps/web/src/shared/lib/api-error.ts` + `api-error.spec.ts`

**Interfaces:**
- Produces:
  - `type ActionResult<T = void> = { ok: true; data: T } | { ok: false; code: ErrorCode; message: string }`
  - `readActionResult<T>(response: Response, parse?: (body: unknown) => T): Promise<ActionResult<T>>`
  - `MESSAGES: Record<ErrorCode, string>` — 코드별 한국어 문구

**스펙 §8.6이 요구한 규약이 여기 한 곳에 산다:** "`ErrorCode`는 contracts에 있어 프론트가 상태 코드가 아니라 코드로 분기한다."

일곱 훅과 여섯 action이 전부 "fetch 하고, 실패면 `ErrorDto`를 파싱해 `code`로 분기한다"를 반복한다. 그 세 줄을 열 번 복제하면 하나만 고쳐지는 날이 온다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```ts
import { ErrorCode } from '@commerce/contracts';
import { describe, expect, it } from 'vitest';
import { MESSAGES, readActionResult } from './api-error';

const jsonResponse = (status: number, body: unknown): Response =>
  new Response(JSON.stringify(body), {
    status,
    headers: { 'Content-Type': 'application/json' },
  });

describe('readActionResult', () => {
  it('204면 ok: true이고 data는 undefined다', async () => {
    const result = await readActionResult(new Response(null, { status: 204 }));
    expect(result).toEqual({ ok: true, data: undefined });
  });

  it('200이고 parse가 있으면 파싱한 값을 담는다', async () => {
    const result = await readActionResult(jsonResponse(200, { id: 'x' }), (body) => body as { id: string });
    expect(result).toEqual({ ok: true, data: { id: 'x' } });
  });

  it('에러 응답의 code로 분기할 수 있다', async () => {
    // 스펙 §8.6: 프론트는 상태 코드가 아니라 코드로 분기한다.
    const result = await readActionResult(
      jsonResponse(409, { code: ErrorCode.INSUFFICIENT_STOCK, message: '재고가 부족합니다: x' }),
    );
    expect(result).toEqual({
      ok: false,
      code: ErrorCode.INSUFFICIENT_STOCK,
      message: MESSAGES[ErrorCode.INSUFFICIENT_STOCK],
    });
  });

  it('서버 메시지가 아니라 우리 문구를 쓴다', async () => {
    // 서버 메시지에는 SKU id 같은 내부 식별자가 들어 있다 — 사용자에게 보일 것이 아니다.
    const result = await readActionResult(
      jsonResponse(409, { code: ErrorCode.INSUFFICIENT_STOCK, message: '재고가 부족합니다: 018f...' }),
    );
    expect(result.ok).toBe(false);
    if (!result.ok) {
      expect(result.message).not.toContain('018f');
    }
  });

  it('알 수 없는 코드는 INTERNAL_ERROR로 떨어진다', async () => {
    const result = await readActionResult(jsonResponse(500, { code: 'WEIRD', message: 'x' }));
    expect(result).toEqual({
      ok: false,
      code: ErrorCode.INTERNAL_ERROR,
      message: MESSAGES[ErrorCode.INTERNAL_ERROR],
    });
  });

  it('본문이 JSON이 아니어도 던지지 않는다', async () => {
    // 프록시의 HTML 502 같은 경우다. 여기서 던지면 화면이 통째로 죽는다.
    const result = await readActionResult(new Response('<html>502</html>', { status: 502 }));
    expect(result).toEqual({
      ok: false,
      code: ErrorCode.INTERNAL_ERROR,
      message: MESSAGES[ErrorCode.INTERNAL_ERROR],
    });
  });

  it('모든 ErrorCode에 문구가 있다', async () => {
    // 하나라도 빠지면 그 에러가 화면에 `undefined`로 나간다.
    for (const code of Object.values(ErrorCode)) {
      expect(MESSAGES[code], code).toBeTruthy();
    }
  });
});
```

- [ ] **Step 2: 실패를 확인한다**

Run: `pnpm vitest run apps/web/src/shared/lib/api-error.spec.ts`
Expected: FAIL — `api-error.ts`가 없다.

- [ ] **Step 3: 구현한다**

```ts
import { ErrorCode, errorDtoSchema } from '@commerce/contracts';

export type ActionResult<T = void> =
  | { readonly ok: true; readonly data: T }
  | { readonly ok: false; readonly code: ErrorCode; readonly message: string };

/**
 * 코드별 사용자 문구. **서버 메시지를 그대로 쓰지 않는다** — 서버 메시지에는
 * SKU id 같은 내부 식별자가 들어 있고(`재고가 부족합니다: 018f2b1c-...`),
 * 그것은 진단용이지 사용자에게 보일 것이 아니다.
 *
 * 모든 `ErrorCode`에 항목이 있어야 한다. 하나라도 빠지면 그 에러가 화면에
 * `undefined`로 나간다 — spec의 마지막 케이스가 그것을 고정한다.
 */
export const MESSAGES: Record<ErrorCode, string> = {
  [ErrorCode.VALIDATION_FAILED]: '입력값을 다시 확인해 주세요.',
  [ErrorCode.UNAUTHENTICATED]: '로그인이 필요합니다.',
  [ErrorCode.FORBIDDEN]: '접근 권한이 없습니다.',
  [ErrorCode.NOT_FOUND]: '찾을 수 없습니다.',
  [ErrorCode.DOMAIN_RULE_VIOLATED]: '요청을 처리할 수 없습니다.',
  [ErrorCode.QUANTITY_BELOW_MINIMUM]: '수량은 1개 이상이어야 합니다.',
  [ErrorCode.INSUFFICIENT_STOCK]: '재고가 부족합니다.',
  [ErrorCode.ORDER_NOT_CANCELLABLE]: '이 주문은 취소할 수 없습니다.',
  [ErrorCode.PAYMENT_DECLINED]: '결제가 거절되었습니다.',
  [ErrorCode.INTERNAL_ERROR]: '일시적인 오류가 발생했습니다. 잠시 후 다시 시도해 주세요.',
  [ErrorCode.EMAIL_ALREADY_REGISTERED]: '이미 가입된 이메일입니다.',
  [ErrorCode.INVALID_CREDENTIALS]: '이메일 또는 비밀번호가 올바르지 않습니다.',
  [ErrorCode.PASSWORD_POLICY_VIOLATED]: '비밀번호가 정책을 만족하지 않습니다.',
};

const failure = (code: ErrorCode): ActionResult<never> => ({
  ok: false,
  code,
  message: MESSAGES[code],
});

async function readJson(response: Response): Promise<unknown> {
  // 프록시의 HTML 502 같은 경우가 있다. 여기서 던지면 화면이 통째로 죽는다.
  try {
    return await response.json();
  } catch {
    return null;
  }
}

/**
 * fetch 응답을 화면이 분기할 수 있는 값으로 바꾼다.
 *
 * **상태 코드로 분기하지 않는다** — 422 하나에 여러 원인이 들어가므로 `code`를 본다
 * (스펙 §8.6). 상태 코드는 성공/실패를 가르는 데만 쓴다.
 */
export async function readActionResult<T = void>(
  response: Response,
  parse?: (body: unknown) => T,
): Promise<ActionResult<T>> {
  if (response.ok) {
    if (parse === undefined) {
      return { ok: true, data: undefined as T };
    }
    return { ok: true, data: parse(await readJson(response)) };
  }

  const parsed = errorDtoSchema.safeParse(await readJson(response));
  return parsed.success ? failure(parsed.data.code) : failure(ErrorCode.INTERNAL_ERROR);
}
```

- [ ] **Step 4: 통과를 확인한다**

Run: `pnpm vitest run apps/web/src/shared/lib/api-error.spec.ts`
Expected: PASS

- [ ] **Step 5: 이 검사가 무엇을 잡는지 증명한다**

**(a) 서버 메시지를 쓰지 않는가**
`failure`를 `(code, serverMessage) => ({ ok: false, code, message: serverMessage })` 형태로 바꿔 서버 메시지를 그대로 쓰게 한다.
Expected: FAIL — `'서버 메시지가 아니라 우리 문구를 쓴다'`가 `'018f'`를 포함한 메시지를 받아 실패한다. **이 회귀는 내부 식별자를 사용자 화면에 노출한다.**
되돌린다.

**(b) JSON이 아닌 본문을 견디는가**
`readJson`의 `try`/`catch`를 걷어낸다.
Expected: FAIL — `'본문이 JSON이 아니어도 던지지 않는다'`가 예외로 실패한다.
되돌린다.

**(c) 문구 누락을 잡는가**
`MESSAGES`에서 `[ErrorCode.PAYMENT_DECLINED]` 줄을 지운다.
Expected: **타입 검사에서 먼저 실패한다** — `Record<ErrorCode, string>`이 전체성을 요구하기 때문이다. `pnpm --filter @commerce/web typecheck`로 확인하고, 그렇다면 `'모든 ErrorCode에 문구가 있다'` 테스트는 타입이 이미 잡는 것을 한 번 더 잡는 셈이다. **그래도 남긴다** — `Record`를 `Partial<Record<...>>`로 느슨하게 바꾸는 리팩터링이 언제든 일어날 수 있고, 그때 이 테스트만이 남는다. 두 관측을 보고서에 적는다.
되돌린다.

- [ ] **Step 6: 전체 검증과 커밋**

```bash
git add apps/web/src/shared/lib
git commit -m "feat(web): 에러 코드 분기를 한 곳에 모은다"
```

---

### Task 3: entities 넷 — 화면에 반복해서 나오는 도메인 조각

**Files:**
- Create: `apps/web/src/entities/product/{ui/ProductCard.tsx, index.ts}` + `ui/ProductCard.spec.tsx`
- Create: `apps/web/src/entities/cart/{ui/CartLineRow.tsx, index.ts}` + `ui/CartLineRow.spec.tsx`
- Create: `apps/web/src/entities/order/{model/order-status.ts, ui/OrderStatusBadge.tsx, index.ts}` + `model/order-status.spec.ts`, `ui/OrderStatusBadge.spec.tsx`
- Create: `apps/web/src/entities/address/{ui/AddressLine.tsx, index.ts}` + `ui/AddressLine.spec.tsx`
- Create: `apps/web/app/globals.css`
- Modify: `apps/web/app/layout.tsx`

**Interfaces:**
- Consumes: `ProductDto`, `CartLineDto`, `OrderDto['status']`, `AddressDto`, `formatMoney`
- Produces:
  - `<ProductCard product={ProductDto} />` — 이름 + 최저가 + 상세 링크
  - `<CartLineRow line={CartLineDto} action?={ReactNode} />` — 이름·단가·수량·소계, 오른쪽에 액션 슬롯
  - `orderStatusLabel(status): string`, `isOrderCancellable(status): boolean`, `<OrderStatusBadge status />`
  - `<AddressLine address={AddressDto} />` — 한 줄 표기

**넷을 한 태스크로 묶는 이유.** 각각 파일 두셋에 렌더링만 하는 조각이고, 리뷰어가 하나만 반려하고 나머지를 승인할 상황이 없다. **`entities/order/model/order-status.ts`만 TDD 대상이다**(스펙 §9.9 — TDD는 `model` 세그먼트에만).

**`isOrderCancellable`이 프론트에 있는 것이 BFF 규칙 위반 아닌가?** 아니다 — 스펙 §8.1이 금지한 것은 "BFF가 계산하는 것"이고 이것은 **화면이 버튼을 그릴지 말지**를 정하는 표현 판단이다. 진짜 규칙은 `Order.cancelBy`가 지키고, 프론트가 틀리면 서버가 409로 거절한다. 그 이중 방어가 정상이다. 화면이 이 판단을 못 하면 취소할 수 없는 주문에도 버튼이 보이고 누르면 에러가 난다.

- [ ] **Step 1: 전역 스타일과 레이아웃**

`globals.css` — 시맨틱 HTML에 최소한의 읽을 만한 기본값만 준다. 클래스 이름을 만들지 않는다(E2E가 `getByRole`로 찾는다).

```css
:root {
  color-scheme: light dark;
  --gap: 1rem;
  --border: 1px solid color-mix(in srgb, currentColor 20%, transparent);
}

body {
  margin: 0 auto;
  max-width: 60rem;
  padding: var(--gap);
  font-family: system-ui, -apple-system, 'Segoe UI', sans-serif;
  line-height: 1.6;
}

header nav {
  display: flex;
  gap: var(--gap);
  align-items: baseline;
  padding-block: var(--gap);
  border-bottom: var(--border);
}

table {
  width: 100%;
  border-collapse: collapse;
}

th,
td {
  padding: 0.5rem;
  text-align: left;
  border-bottom: var(--border);
}

button {
  padding: 0.4rem 0.8rem;
  font: inherit;
  cursor: pointer;
}

button:disabled {
  cursor: progress;
  opacity: 0.6;
}

[role='alert'] {
  padding: 0.5rem;
  border: var(--border);
}

ul[role='list'] {
  padding: 0;
  list-style: none;
}
```

`layout.tsx`:

```tsx
import { Header } from '@/widgets/header';
import './globals.css';

export const metadata = { title: 'Commerce' };

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="ko">
      <body>
        <Header />
        <main>{children}</main>
      </body>
    </html>
  );
}
```

**`Header`는 태스크 9가 만든다.** 이 태스크에서는 `layout.tsx`를 아직 고치지 않고 `globals.css`만 만든 뒤, 태스크 9에서 함께 배선한다 — 없는 모듈을 import하면 빌드가 깨진다.

- [ ] **Step 2: `order-status` 모델의 실패하는 테스트를 쓴다**

```ts
import { describe, expect, it } from 'vitest';
import { isOrderCancellable, ORDER_STATUS_LABELS, orderStatusLabel } from './order-status';

describe('orderStatusLabel', () => {
  it('상태를 한국어로 옮긴다', () => {
    expect(orderStatusLabel('PENDING_PAYMENT')).toBe('결제 대기');
    expect(orderStatusLabel('PAID')).toBe('결제 완료');
    expect(orderStatusLabel('PAYMENT_FAILED')).toBe('결제 실패');
    expect(orderStatusLabel('CANCELLED')).toBe('취소됨');
    expect(orderStatusLabel('REFUND_PENDING')).toBe('환불 처리 중');
    expect(orderStatusLabel('REFUNDED')).toBe('환불 완료');
  });

  it('모든 상태에 라벨이 있다', () => {
    // 하나라도 빠지면 그 상태의 주문 화면에 `undefined`가 나간다.
    // 계약의 열거값을 출처로 삼아 화면이 뒤처지지 않게 한다.
    for (const status of orderStatusSchema.options) {
      expect(ORDER_STATUS_LABELS[status], status).toBeTruthy();
    }
  });
});

describe('isOrderCancellable', () => {
  it('결제 전과 결제 후에는 취소할 수 있다', () => {
    expect(isOrderCancellable('PENDING_PAYMENT')).toBe(true);
    expect(isOrderCancellable('PAID')).toBe(true);
  });

  it('이미 결말이 난 주문은 취소할 수 없다', () => {
    // 서버의 Order.cancelBy가 같은 규칙을 지킨다 — 여기는 버튼을 그릴지 말지의
    // 표현 판단이고, 프론트가 틀리면 서버가 409로 거절한다.
    expect(isOrderCancellable('PAYMENT_FAILED')).toBe(false);
    expect(isOrderCancellable('CANCELLED')).toBe(false);
    expect(isOrderCancellable('REFUNDED')).toBe(false);
  });

  it('환불 처리 중에는 취소 버튼을 다시 보여주지 않는다', () => {
    // 서버는 false를 돌려주는 멱등 연산이라 눌러도 안전하지만, 사용자에게
    // "취소" 버튼을 다시 보여주면 이미 취소했다는 사실이 전달되지 않는다.
    expect(isOrderCancellable('REFUND_PENDING')).toBe(false);
  });
});
```

`orderStatusSchema` import를 파일 상단에 더한다: `import { orderStatusSchema } from '@commerce/contracts';`

- [ ] **Step 3: 실패를 확인한다**

Run: `pnpm vitest run apps/web/src/entities/order`
Expected: FAIL — `order-status.ts`가 없다.

- [ ] **Step 4: entities 넷을 구현한다**

`entities/order/model/order-status.ts`:

```ts
import type { OrderDto } from '@commerce/contracts';

type OrderStatus = OrderDto['status'];

export const ORDER_STATUS_LABELS: Record<OrderStatus, string> = {
  PENDING_PAYMENT: '결제 대기',
  PAID: '결제 완료',
  PAYMENT_FAILED: '결제 실패',
  CANCELLED: '취소됨',
  // 계획 4의 편차 1이 더한 상태. 취소 요청과 환불 완료 사이다.
  REFUND_PENDING: '환불 처리 중',
  REFUNDED: '환불 완료',
};

export function orderStatusLabel(status: OrderStatus): string {
  return ORDER_STATUS_LABELS[status];
}

/**
 * 취소 버튼을 그릴지 말지. **표현 판단이지 도메인 규칙이 아니다** —
 * 진짜 규칙은 `Order.cancelBy`가 지키고, 여기가 틀리면 서버가 409로 거절한다.
 * 그 이중 방어가 정상이다(스펙 §8.1은 BFF가 *계산*하는 것을 금지했고, 이것은
 * 계산이 아니라 버튼 노출 여부다).
 *
 * `REFUND_PENDING`을 제외하는 이유: 서버는 멱등해서 눌러도 안전하지만, 취소
 * 버튼을 다시 보여주면 이미 취소했다는 사실이 사용자에게 전달되지 않는다.
 */
export function isOrderCancellable(status: OrderStatus): boolean {
  return status === 'PENDING_PAYMENT' || status === 'PAID';
}
```

`entities/order/ui/OrderStatusBadge.tsx`:

```tsx
import type { OrderDto } from '@commerce/contracts';
import { orderStatusLabel } from '../model/order-status';

export function OrderStatusBadge({ status }: { status: OrderDto['status'] }) {
  // data-status는 E2E와 컴포넌트 테스트가 라벨 문구에 묶이지 않게 하는 고리다.
  return <span data-status={status}>{orderStatusLabel(status)}</span>;
}
```

`entities/order/index.ts`:

```ts
export { isOrderCancellable, orderStatusLabel } from './model/order-status';
export { OrderStatusBadge } from './ui/OrderStatusBadge';
```

`entities/product/ui/ProductCard.tsx`:

```tsx
import type { ProductDto } from '@commerce/contracts';
import Link from 'next/link';
import { formatMoney } from '@/shared/lib/format-money';

/**
 * 목록에 쓰는 카드. **최저가를 계산해 보여준다** — 이것은 표현 로직이다.
 * 주문 금액 같은 진짜 계산은 서버가 하고 API가 계산된 값을 내려준다(스펙 §8.1).
 */
export function ProductCard({ product }: { product: ProductDto }) {
  const cheapest = product.skus.reduce((min, sku) =>
    BigInt(sku.price.amount) < BigInt(min.price.amount) ? sku : min,
  );

  return (
    <article>
      <h3>
        <Link href={`/products/${product.id}`}>{product.name}</Link>
      </h3>
      <p>{formatMoney(cheapest.price)}부터</p>
    </article>
  );
}
```

`entities/cart/ui/CartLineRow.tsx`:

```tsx
import type { CartLineDto } from '@commerce/contracts';
import type { ReactNode } from 'react';
import { formatMoney } from '@/shared/lib/format-money';

/**
 * `action` 슬롯이 있는 이유: 장바구니 화면은 "빼기" 버튼을, 주문 요약은 아무것도
 * 넣지 않는다. 슬롯이 없으면 두 줄 컴포넌트가 생기고 표기가 갈라진다.
 */
export function CartLineRow({ line, action }: { line: CartLineDto; action?: ReactNode }) {
  return (
    <tr>
      <td>{line.nameSnapshot}</td>
      <td>{formatMoney(line.unitPrice)}</td>
      <td>{line.quantity}개</td>
      <td>{formatMoney(line.subtotal)}</td>
      {action === undefined ? null : <td>{action}</td>}
    </tr>
  );
}
```

`entities/address/ui/AddressLine.tsx`:

```tsx
import type { AddressDto } from '@commerce/contracts';

export function AddressLine({ address }: { address: AddressDto }) {
  return (
    <span>
      {address.recipient} · {address.phone} · [{address.zip}] {address.line1}
      {address.line2 === undefined ? '' : ` ${address.line2}`}
    </span>
  );
}
```

각 슬라이스의 `index.ts`는 `ui/`의 컴포넌트만 내보낸다.

- [ ] **Step 5: 컴포넌트 테스트를 쓴다 (test-after)**

스펙 §9.9: UI에는 TDD를 적용하지 않는다. 구현 뒤에 **행동만** 검증한다 — 레이아웃과 클래스는 보지 않는다.

```tsx
// ProductCard.spec.tsx
import { render, screen } from '@testing-library/react';
import { describe, expect, it } from 'vitest';
import { aProductDto, KRW } from '@/shared/api/msw/fixtures';
import { ProductCard } from './ProductCard';

describe('ProductCard', () => {
  it('이름과 상세 링크를 보여준다', () => {
    const product = aProductDto();
    render(<ProductCard product={product} />);

    expect(screen.getByRole('link', { name: '티셔츠' })).toHaveAttribute(
      'href',
      `/products/${product.id}`,
    );
  });

  it('가장 싼 SKU의 가격을 보여준다', () => {
    // 12000과 13000 중 12000. bigint 비교라 문자열 정렬 함정을 피한다 —
    // '9000' > '13000'이 문자열로는 참이다.
    render(
      <ProductCard
        product={aProductDto({
          skus: [
            { id: 'a', code: 'A', price: KRW('13000') },
            { id: 'b', code: 'B', price: KRW('9000') },
          ] as never,
        })}
      />,
    );

    expect(screen.getByText('9,000원부터')).toBeInTheDocument();
  });
});
```

`CartLineRow.spec.tsx`는 `<table><tbody>`로 감싸 렌더하고 이름·수량·소계가 보이는지, `action`을 주면 그 노드가 렌더되고 안 주면 셀이 없는지 확인한다.

`OrderStatusBadge.spec.tsx`는 `data-status` 속성과 한국어 라벨이 함께 나오는지 확인한다.

`AddressLine.spec.tsx`는 `line2`가 있을 때와 없을 때를 확인한다.

- [ ] **Step 6: 통과를 확인한다**

Run: `pnpm vitest run apps/web/src/entities`
Expected: PASS

- [ ] **Step 7: 이 검사가 무엇을 잡는지 증명한다**

**(a) 최저가 계산이 bigint 비교인가**
`ProductCard`의 `BigInt(sku.price.amount) < BigInt(min.price.amount)`를 `sku.price.amount < min.price.amount`(문자열 비교)로 바꾼다.
Expected: FAIL — `'가장 싼 SKU의 가격을 보여준다'`가 `'13,000원부터'`를 받아 실패한다. `'13000' < '9000'`이 문자열로는 참이기 때문이다. **금액을 문자열로 비교하는 것은 커머스에서 가장 흔한 조용한 버그다.**
되돌린다.

**(b) 라벨 전체성 검사가 도는가**
`ORDER_STATUS_LABELS`에서 `REFUND_PENDING` 줄을 지운다.
Expected: 타입 검사(`Record<OrderStatus, string>`)가 먼저 실패한다. 태스크 2의 (c)와 같은 관측이고, 같은 이유로 테스트를 남긴다. 두 결과를 보고서에 적는다.
되돌린다.

**(c) `isOrderCancellable`이 `REFUND_PENDING`을 제외하는가**
`REFUND_PENDING`을 `true`로 바꾼다.
Expected: FAIL — `'환불 처리 중에는 취소 버튼을 다시 보여주지 않는다'`가 실패한다.
되돌린다.

- [ ] **Step 8: 전체 검증과 커밋**

Run: `pnpm verify`
Expected: exit 0. `arch:check`가 통과한다 — entities는 features·widgets·views를 import하지 않는다.

```bash
git add apps/web/src/entities apps/web/app/globals.css
git commit -m "feat(web): 상품·장바구니·주문·배송지 entity 컴포넌트를 추가한다"
```

---

### Task 4: BFF — 장바구니·배송지 action과 Route Handler

**Files:**
- Create: `apps/web/src/server/bff-deps.ts`
- Create: `apps/web/src/server/cart-actions.ts` + `cart-actions.spec.ts`
- Create: `apps/web/src/server/address-actions.ts` + `address-actions.spec.ts`
- Create: `apps/web/app/api/cart/items/route.ts`
- Create: `apps/web/app/api/cart/items/[skuId]/route.ts`
- Create: `apps/web/app/api/addresses/route.ts`

**Interfaces:**
- Consumes: `TokenStore`, `createAuthenticatedApi`, `SessionExpiredError`, `ActionResult`, `readActionResult`
- Produces:
  - `addCartItemAction(input: AddCartItemBody, deps: BffDeps): Promise<ActionResult>`
  - `changeCartItemAction(skuId: string, input: ChangeCartItemBody, deps): Promise<ActionResult>`
  - `removeCartItemAction(skuId: string, deps): Promise<ActionResult>`
  - `addAddressAction(input: AddressBody, deps): Promise<ActionResult<AddressDto>>`
  - `interface BffDeps { baseUrl: string; store: TokenStore }` (in `bff-deps.ts`)
  - `toResponse(result: ActionResult<unknown>): Response` — Route Handler 공용

**계획 1의 `auth-actions.ts`가 세운 형태를 그대로 따른다.** 로직은 action에, Route Handler는 접착제 3줄. 그래야 `TokenStore`를 주입해 테스트할 수 있다 — Route Handler를 vitest에서 부르려면 Next의 요청 컨텍스트가 필요하고 그걸 흉내내려면 목 라이브러리가 든다(금지).

**action이 `fetch`를 직접 쓰지 않고 `createAuthenticatedApi`를 쓰는 이유.** 그 안에 401 → refresh → 1회 재시도가 들어 있다(스펙 §8.5). 직접 `fetch`하면 그 로직이 여덟 곳에 복제되거나 아예 빠진다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`cart-actions.spec.ts` — MSW가 네트워크를 세우고 `InMemoryTokenStore`(계획 1)가 토큰을 준다.

```ts
import { ErrorCode } from '@commerce/contracts';
import { HttpResponse, http } from 'msw';
import { describe, expect, it } from 'vitest';
import { SKU_ID } from '../shared/api/msw/fixtures';
import { server } from '../shared/api/msw/server';
import { addCartItemAction, changeCartItemAction, removeCartItemAction } from './cart-actions';
import { SessionExpiredError } from './api-client';
import { InMemoryTokenStore } from './testing/in-memory-token-store';

const BASE = process.env['API_BASE_URL'] ?? 'http://localhost:3001';

const deps = (tokens: { accessToken: string; refreshToken: string } | null = {
  accessToken: 'a',
  refreshToken: 'r',
}) => ({ baseUrl: BASE, store: new InMemoryTokenStore(tokens) });

describe('addCartItemAction', () => {
  it('성공하면 ok: true다', async () => {
    const result = await addCartItemAction({ skuId: SKU_ID, quantity: 2 }, deps());
    expect(result).toEqual({ ok: true, data: undefined });
  });

  it('액세스 토큰을 헤더에 싣는다', async () => {
    // 이것이 BFF의 존재 이유다 — 브라우저는 토큰을 보지 않는다(스펙 §8.5).
    let seen: string | null = null;
    server.use(
      http.post(`${BASE}/cart/items`, ({ request }) => {
        seen = request.headers.get('authorization');
        return new HttpResponse(null, { status: 204 });
      }),
    );

    await addCartItemAction({ skuId: SKU_ID, quantity: 1 }, deps());

    expect(seen).toBe('Bearer a');
  });

  it('재고 부족이면 code로 분기할 수 있다', async () => {
    server.use(
      http.post(`${BASE}/cart/items`, () =>
        HttpResponse.json({ code: ErrorCode.INSUFFICIENT_STOCK, message: '재고 부족: 018f' }, { status: 409 }),
      ),
    );

    const result = await addCartItemAction({ skuId: SKU_ID, quantity: 1 }, deps());

    expect(result.ok).toBe(false);
    if (!result.ok) {
      expect(result.code).toBe(ErrorCode.INSUFFICIENT_STOCK);
    }
  });

  it('세션이 없으면 SessionExpiredError다', async () => {
    // 호출자(Route Handler)가 이걸 잡아 401로 바꾼다.
    await expect(addCartItemAction({ skuId: SKU_ID, quantity: 1 }, deps(null))).rejects.toThrow(
      SessionExpiredError,
    );
  });
});

describe('changeCartItemAction', () => {
  it('경로에 skuId를 넣는다', async () => {
    let seenUrl: string | null = null;
    server.use(
      http.put(`${BASE}/cart/items/:skuId`, ({ request }) => {
        seenUrl = request.url;
        return new HttpResponse(null, { status: 204 });
      }),
    );

    await changeCartItemAction(SKU_ID, { quantity: 5 }, deps());

    expect(seenUrl).toContain(`/cart/items/${SKU_ID}`);
  });

  it('없는 줄이면 NOT_FOUND로 분기할 수 있다', async () => {
    server.use(
      http.put(`${BASE}/cart/items/:skuId`, () =>
        HttpResponse.json({ code: ErrorCode.NOT_FOUND, message: 'x' }, { status: 404 }),
      ),
    );

    const result = await changeCartItemAction(SKU_ID, { quantity: 5 }, deps());

    expect(result.ok).toBe(false);
    if (!result.ok) {
      expect(result.code).toBe(ErrorCode.NOT_FOUND);
    }
  });
});

describe('removeCartItemAction', () => {
  it('성공하면 ok: true다', async () => {
    expect(await removeCartItemAction(SKU_ID, deps())).toEqual({ ok: true, data: undefined });
  });
});
```

`address-actions.spec.ts`는 (1) 생성 성공 시 `AddressDto`를 `data`에 담는지, (2) 400이면 `VALIDATION_FAILED`로 분기되는지, (3) 세션이 없으면 `SessionExpiredError`인지를 덮는다.

`InMemoryTokenStore`는 계획 1이 만든 것이다 — `constructor(tokens: Tokens | null = null)`이고 `writes`·`clearCalls`로 호출 이력을 노출한다.

- [ ] **Step 2: 실패를 확인한다**

Run: `pnpm vitest run apps/web/src/server/cart-actions.spec.ts`
Expected: FAIL — `cart-actions.ts`가 없다.

- [ ] **Step 3: 구현한다**

**먼저 `api-client.ts`에 `authedFetch`를 더한다.** 기존 `createAuthenticatedApi`는 ts-rest 클라이언트용(`ApiFetcher` 인터페이스)이라 action이 직접 부르기에 맞지 않는다 — action은 계약의 라우트 이름이 아니라 **경로와 본문**만 알면 되고, ts-rest 클라이언트를 쓰면 반환 타입이 라우트별 판별 유니온이라 `readActionResult`로 넘기기 전에 라우트마다 손으로 풀어야 한다.

두 함수가 401 재시도 로직을 공유하되 시그니처가 다르다.

```ts
/**
 * 토큰 주입 + 401 시 갱신 후 **정확히 1회** 재시도를 담은 `fetch`(스펙 §8.5).
 * Route Handler용 action들이 쓴다.
 *
 * `createAuthenticatedApi`(ts-rest의 `ApiFetcher`)와 같은 로직이지만 시그니처가
 * `fetch`에 가깝다 — action은 계약의 라우트 이름이 아니라 경로와 본문만 알면 된다.
 * 두 함수가 같은 규칙을 갖는 한 401 재시도 테스트가 **양쪽에 다 있어야 한다.**
 */
export function authedFetch(baseUrl: string, store: TokenStore) {
  return async (path: string, init: RequestInit = {}): Promise<Response> => {
    const tokens = await store.read();
    if (tokens === null) {
      throw new SessionExpiredError('세션이 없습니다.');
    }

    const send = (accessToken: string) =>
      fetch(`${baseUrl}${path}`, {
        ...init,
        headers: {
          'Content-Type': 'application/json',
          ...init.headers,
          authorization: `Bearer ${accessToken}`,
        },
      });

    const first = await send(tokens.accessToken);
    if (first.status !== 401) {
      return first;
    }

    // 401이 아닌 오류(500 등)에는 갱신하지 않는다 — 멀쩡한 리프레시 토큰을
    // 회전시켜 태울 이유가 없다.
    const refreshed = await refreshTokens(baseUrl, tokens.refreshToken);
    if (refreshed === null) {
      await store.clear();
      throw new SessionExpiredError('세션 갱신에 실패했습니다.');
    }
    await store.write(refreshed);

    const second = await send(refreshed.accessToken);
    if (second.status === 401) {
      await store.clear();
      throw new SessionExpiredError('갱신 후에도 인증에 실패했습니다.');
    }
    return second;
  };
}
```

`api-client.spec.ts`에 `authedFetch`의 401 재시도 케이스 셋을 더한다 — 첫 401 → 갱신 → 재시도 성공 / 갱신 실패 → `SessionExpiredError` + `store.clearCalls === 1` / 갱신 후에도 401 → 세션 폐기. `createAuthenticatedApi`에 이미 있는 것과 같은 셋이고, 두 함수가 갈라지는 순간을 잡는 것이 목적이다.

그 위에 action은 얇다.

```ts
// cart-actions.ts
import 'server-only';
import type { AddCartItemBody, ChangeCartItemBody } from '@commerce/contracts';
import { type ActionResult, readActionResult } from '../shared/lib/api-error';
import { authedFetch } from './api-client';
import type { BffDeps } from './bff-deps';

export async function addCartItemAction(
  input: AddCartItemBody,
  deps: BffDeps,
): Promise<ActionResult> {
  const response = await authedFetch(deps.baseUrl, deps.store)('/cart/items', {
    method: 'POST',
    body: JSON.stringify(input),
  });
  return readActionResult(response);
}

export async function changeCartItemAction(
  skuId: string,
  input: ChangeCartItemBody,
  deps: BffDeps,
): Promise<ActionResult> {
  const response = await authedFetch(deps.baseUrl, deps.store)(`/cart/items/${skuId}`, {
    method: 'PUT',
    body: JSON.stringify(input),
  });
  return readActionResult(response);
}

export async function removeCartItemAction(skuId: string, deps: BffDeps): Promise<ActionResult> {
  const response = await authedFetch(deps.baseUrl, deps.store)(`/cart/items/${skuId}`, {
    method: 'DELETE',
  });
  return readActionResult(response);
}
```

`address-actions.ts`는 같은 형태이고 `POST /addresses`에 `addressDtoSchema`로 응답을 파싱해 `data`에 담는다.

**버린 대안**: 계약 클라이언트(`createApiClient`)로 `client.cart.addItem({ body })`를 부르는 것. 타입은 가장 안전하지만 반환값이 라우트별 판별 유니온이라 `readActionResult`가 받지 못하고, 라우트마다 `status` 분기를 손으로 써야 한다 — 여덟 곳에서 그 분기를 반복하면 스펙 §8.6이 "코드로 분기한다"고 정한 규약이 흩어진다.

- [ ] **Step 4: Route Handler를 쓴다**

`app/api/cart/items/route.ts`:

```ts
import { addCartItemBodySchema } from '@commerce/contracts';
import { addCartItemAction } from '@/server/cart-actions';
import { apiBaseUrl } from '@/server/api-client';
import { cookieTokenStore } from '@/server/session';
import { toResponse } from '@/server/bff-response';

/**
 * 접착제. 로직은 전부 `addCartItemAction`에 있고 그쪽은 테스트가 있다.
 * 이 파일 자체에는 자동 테스트가 없다 — Route Handler를 vitest에서 부르려면
 * Next의 요청 컨텍스트가 필요하고 그걸 흉내내려면 목 라이브러리가 든다(금지).
 * 태스크 13~14의 Playwright E2E가 이 경로를 덮는다.
 */
export async function POST(request: Request): Promise<Response> {
  const body = addCartItemBodySchema.safeParse(await request.json());
  if (!body.success) {
    return Response.json({ code: 'VALIDATION_FAILED', message: '입력값이 올바르지 않습니다.' }, { status: 400 });
  }
  return toResponse(
    await addCartItemAction(body.data, {
      baseUrl: apiBaseUrl(),
      store: await cookieTokenStore(),
    }),
  );
}
```

`src/server/bff-response.ts` + spec:

```ts
import 'server-only';
import { ErrorCode } from '@commerce/contracts';
import { SessionExpiredError } from './api-client';
import type { ActionResult } from '../shared/lib/api-error';

/**
 * `ActionResult`를 HTTP 응답으로 바꾼다. 코드→상태 매핑이 여기 한 곳에 산다.
 *
 * **BFF는 계산하지 않는다**(스펙 §8.1) — 이 매핑은 계산이 아니라 형태 변환이다.
 * 어느 코드가 어느 상태인지는 이미 Nest가 정했고, 여기서는 그것을 브라우저가
 * 이해하는 모양으로 옮길 뿐이다.
 */
const STATUS: Partial<Record<ErrorCode, number>> = {
  [ErrorCode.VALIDATION_FAILED]: 400,
  [ErrorCode.UNAUTHENTICATED]: 401,
  [ErrorCode.INVALID_CREDENTIALS]: 401,
  [ErrorCode.FORBIDDEN]: 403,
  [ErrorCode.NOT_FOUND]: 404,
  [ErrorCode.INSUFFICIENT_STOCK]: 409,
  [ErrorCode.ORDER_NOT_CANCELLABLE]: 409,
  [ErrorCode.EMAIL_ALREADY_REGISTERED]: 409,
  [ErrorCode.DOMAIN_RULE_VIOLATED]: 422,
  [ErrorCode.QUANTITY_BELOW_MINIMUM]: 422,
  [ErrorCode.PASSWORD_POLICY_VIOLATED]: 422,
  [ErrorCode.PAYMENT_DECLINED]: 422,
};

export function toResponse(result: ActionResult<unknown>): Response {
  if (result.ok) {
    return result.data === undefined
      ? new Response(null, { status: 204 })
      : Response.json(result.data, { status: 200 });
  }
  return Response.json(
    { code: result.code, message: result.message },
    { status: STATUS[result.code] ?? 500 },
  );
}

/**
 * 세션 만료를 401로 바꾼다. 각 Route Handler가 `try`/`catch`를 쓰는 대신
 * 이 래퍼를 통과시킨다 — 여덟 곳에 같은 `catch`를 쓰면 하나를 빠뜨린다.
 */
export async function handleBff(work: () => Promise<Response>): Promise<Response> {
  try {
    return await work();
  } catch (error) {
    if (error instanceof SessionExpiredError) {
      return Response.json(
        { code: ErrorCode.UNAUTHENTICATED, message: '로그인이 필요합니다.' },
        { status: 401 },
      );
    }
    throw error;
  }
}
```

`bff-response.spec.ts`가 덮는 것: `ok: true`이고 `data`가 `undefined`면 204 / `data`가 있으면 200과 본문 / 각 `ErrorCode`가 기대한 상태로 나가는지 / 매핑에 없는 코드는 500 / `handleBff`가 `SessionExpiredError`를 401로 바꾸고 다른 예외는 그대로 던지는지.

모든 Route Handler는 `handleBff(async () => ...)`로 감싼다.

`app/api/cart/items/[skuId]/route.ts`는 `PUT`과 `DELETE`를 갖고, `context.params`에서 `skuId`를 꺼낸다(Next 16은 `params`가 Promise다 — `const { skuId } = await context.params;`).

- [ ] **Step 5: 통과를 확인한다**

Run: `pnpm vitest run apps/web/src/server`
Expected: PASS

- [ ] **Step 6: 이 검사가 무엇을 잡는지 증명한다**

**(a) 토큰이 실제로 실리는가**
`authedFetch`의 `authorization: \`Bearer ${accessToken}\`` 줄을 지운다.
Expected: FAIL — `'액세스 토큰을 헤더에 싣는다'`가 `null`을 받아 실패한다. **이 회귀는 모든 변경 요청을 401로 만든다.**
되돌린다.

**(b) 세션 없음을 던지는가**
`authedFetch`의 `if (tokens === null) throw ...`를 지우고 빈 토큰으로 진행하게 한다.
Expected: FAIL — `'세션이 없으면 SessionExpiredError다'`가 실패한다.
되돌린다.

**(c) 상태 매핑이 실제로 쓰이는가**
`toResponse`의 `STATUS[result.code] ?? 500`을 `400`으로 고정한다.
Expected: FAIL — `bff-response.spec.ts`의 코드별 상태 케이스가 실패한다. **이 회귀는 재고 부족(409)과 인증 실패(401)를 전부 400으로 만들어 화면이 분기하지 못하게 한다.**
되돌린다.

- [ ] **Step 7: 전체 검증과 커밋**

Run: `pnpm verify`
Expected: exit 0.

```bash
git add apps/web/src/server apps/web/app/api
git commit -m "feat(web): 장바구니·배송지 BFF action과 Route Handler를 추가한다"
```

---

### Task 5: BFF — 주문 action과 Route Handler

**Files:**
- Create: `apps/web/src/server/order-actions.ts` + `order-actions.spec.ts`
- Create: `apps/web/app/api/orders/route.ts`
- Create: `apps/web/app/api/orders/[orderId]/cancel/route.ts`

**Interfaces:**
- Consumes: `authedFetch`, `readActionResult`, `toResponse`, `handleBff`, `BffDeps`
- Produces:
  - `placeOrderAction(input: PlaceOrderBody, deps: BffDeps): Promise<ActionResult<PlaceOrderResultDto>>`
  - `cancelOrderAction(orderId: string, deps: BffDeps): Promise<ActionResult<CancelOrderResultDto>>`

**주문을 별도 태스크로 두는 이유.** 장바구니는 204만 돌려주지만 주문은 **본문을 파싱해 돌려준다** — 그리고 그 본문이 사가의 결과다. `PAYMENT_FAILED`가 201로 오는 것(계획 4의 결정)을 프론트가 성공으로 받아 상태로 분기해야 하고, 그 판단이 이 태스크의 내용이다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```ts
import { ErrorCode } from '@commerce/contracts';
import { HttpResponse, http } from 'msw';
import { describe, expect, it } from 'vitest';
import { ADDRESS_ID, ORDER_ID } from '../shared/api/msw/fixtures';
import { server } from '../shared/api/msw/server';
import { cancelOrderAction, placeOrderAction } from './order-actions';
import { InMemoryTokenStore } from './testing/in-memory-token-store';

const BASE = process.env['API_BASE_URL'] ?? 'http://localhost:3001';
const deps = () => ({
  baseUrl: BASE,
  store: new InMemoryTokenStore({ accessToken: 'a', refreshToken: 'r' }),
});

describe('placeOrderAction', () => {
  it('결제 성공이면 orderId와 PAID를 담아 돌려준다', async () => {
    const result = await placeOrderAction({ addressId: ADDRESS_ID }, deps());

    expect(result).toEqual({ ok: true, data: { orderId: ORDER_ID, status: 'PAID' } });
  });

  it('결제 거절도 성공 응답이다 — 상태로 분기한다', async () => {
    // 계획 4의 결정: 주문은 만들어졌고 주문 번호가 있다. 4xx로 만들면 클라이언트가
    // 번호를 받지 못해 "다시 시도" 화면을 그릴 수 없다.
    server.use(
      http.post(`${BASE}/orders`, () =>
        HttpResponse.json({ orderId: ORDER_ID, status: 'PAYMENT_FAILED' }, { status: 201 }),
      ),
    );

    const result = await placeOrderAction({ addressId: ADDRESS_ID }, deps());

    expect(result.ok).toBe(true);
    if (result.ok) {
      expect(result.data.status).toBe('PAYMENT_FAILED');
    }
  });

  it('재고 부족은 실패이고 code로 분기할 수 있다', async () => {
    // 예약 단계 실패는 주문이 완성되기 전이라 409로 나온다(계획 4).
    server.use(
      http.post(`${BASE}/orders`, () =>
        HttpResponse.json(
          { code: ErrorCode.INSUFFICIENT_STOCK, message: '재고가 부족합니다: 018f' },
          { status: 409 },
        ),
      ),
    );

    const result = await placeOrderAction({ addressId: ADDRESS_ID }, deps());

    expect(result.ok).toBe(false);
    if (!result.ok) {
      expect(result.code).toBe(ErrorCode.INSUFFICIENT_STOCK);
    }
  });

  it('빈 장바구니는 DOMAIN_RULE_VIOLATED다', async () => {
    server.use(
      http.post(`${BASE}/orders`, () =>
        HttpResponse.json({ code: ErrorCode.DOMAIN_RULE_VIOLATED, message: 'x' }, { status: 422 }),
      ),
    );

    const result = await placeOrderAction({ addressId: ADDRESS_ID }, deps());

    expect(result.ok).toBe(false);
  });

  it('응답이 계약 형태가 아니면 실패로 만든다', async () => {
    // 서버가 형태를 바꾸면 여기서 즉시 드러난다 — undefined orderId로 라우팅하는
    // 것보다 낫다. 계획 1의 `auth-actions.ts`가 같은 판단을 했다.
    server.use(
      http.post(`${BASE}/orders`, () => HttpResponse.json({ weird: true }, { status: 201 })),
    );

    const result = await placeOrderAction({ addressId: ADDRESS_ID }, deps());

    expect(result.ok).toBe(false);
    if (!result.ok) {
      expect(result.code).toBe(ErrorCode.INTERNAL_ERROR);
    }
  });
});

describe('cancelOrderAction', () => {
  it('취소하면 결과 상태를 담아 돌려준다', async () => {
    const result = await cancelOrderAction(ORDER_ID, deps());

    expect(result).toEqual({ ok: true, data: { status: 'REFUND_PENDING' } });
  });

  it('취소할 수 없는 주문은 ORDER_NOT_CANCELLABLE이다', async () => {
    server.use(
      http.post(`${BASE}/orders/${ORDER_ID}/cancel`, () =>
        HttpResponse.json({ code: ErrorCode.ORDER_NOT_CANCELLABLE, message: 'x' }, { status: 409 }),
      ),
    );

    const result = await cancelOrderAction(ORDER_ID, deps());

    expect(result.ok).toBe(false);
    if (!result.ok) {
      expect(result.code).toBe(ErrorCode.ORDER_NOT_CANCELLABLE);
    }
  });

  it('남의 주문은 FORBIDDEN이다', async () => {
    server.use(
      http.post(`${BASE}/orders/${ORDER_ID}/cancel`, () =>
        HttpResponse.json({ code: ErrorCode.FORBIDDEN, message: 'x' }, { status: 403 }),
      ),
    );

    const result = await cancelOrderAction(ORDER_ID, deps());

    expect(result.ok).toBe(false);
    if (!result.ok) {
      expect(result.code).toBe(ErrorCode.FORBIDDEN);
    }
  });
});
```

- [ ] **Step 2: 실패를 확인한다**

Run: `pnpm vitest run apps/web/src/server/order-actions.spec.ts`
Expected: FAIL — `order-actions.ts`가 없다.

- [ ] **Step 3: 구현한다**

```ts
import 'server-only';
import {
  cancelOrderResultSchema,
  type CancelOrderResultDto,
  type PlaceOrderBody,
  placeOrderResultSchema,
  type PlaceOrderResultDto,
} from '@commerce/contracts';
import { ErrorCode } from '@commerce/contracts';
import { type ActionResult, MESSAGES, readActionResult } from '../shared/lib/api-error';
import { authedFetch } from './api-client';
import type { BffDeps } from './bff-deps';

const shapeMismatch = (): ActionResult<never> => ({
  ok: false,
  code: ErrorCode.INTERNAL_ERROR,
  message: MESSAGES[ErrorCode.INTERNAL_ERROR],
});

/**
 * 주문한다. **거절도 성공 응답이다** — 계획 4의 결정: 주문은 만들어졌고 번호가 있다.
 * 화면이 `data.status`로 분기해 "결제가 거절되었습니다"를 그린다.
 *
 * 응답을 계약 스키마로 파싱한다. 서버가 형태를 바꾸면 여기서 즉시 드러난다 —
 * `undefined` orderId로 라우팅하는 것보다 낫다.
 */
export async function placeOrderAction(
  input: PlaceOrderBody,
  deps: BffDeps,
): Promise<ActionResult<PlaceOrderResultDto>> {
  const response = await authedFetch(deps.baseUrl, deps.store)('/orders', {
    method: 'POST',
    body: JSON.stringify(input),
  });
  const result = await readActionResult<unknown>(response, (body) => body);
  if (!result.ok) {
    return result;
  }
  const parsed = placeOrderResultSchema.safeParse(result.data);
  return parsed.success ? { ok: true, data: parsed.data } : shapeMismatch();
}

export async function cancelOrderAction(
  orderId: string,
  deps: BffDeps,
): Promise<ActionResult<CancelOrderResultDto>> {
  const response = await authedFetch(deps.baseUrl, deps.store)(`/orders/${orderId}/cancel`, {
    method: 'POST',
  });
  const result = await readActionResult<unknown>(response, (body) => body);
  if (!result.ok) {
    return result;
  }
  const parsed = cancelOrderResultSchema.safeParse(result.data);
  return parsed.success ? { ok: true, data: parsed.data } : shapeMismatch();
}
```

**`BffDeps`는 `src/server/bff-deps.ts`에 둔다.** 세 action 파일이 같은 타입을 쓰는데 하나가 다른 하나를 import하면 의존 방향이 임의로 생긴다 — `order-actions`가 `cart-actions`를 아는 이유가 없다.

```ts
// src/server/bff-deps.ts
import type { TokenStore } from './token-store';

/** 세 action 파일이 공유하는 주입 형태. 어느 한쪽이 다른 쪽을 import하지 않게 뽑았다. */
export interface BffDeps {
  readonly baseUrl: string;
  readonly store: TokenStore;
}
```

태스크 4의 `cart-actions.ts`에서도 이 파일에서 import한다(위 코드의 `export interface BffDeps`를 지우고).

- [ ] **Step 4: Route Handler를 쓴다**

`app/api/orders/route.ts`와 `app/api/orders/[orderId]/cancel/route.ts`. 둘 다 태스크 4의 형태를 따르고 `handleBff`로 감싼다. 취소는 본문이 없으므로 `request.json()`을 부르지 않는다.

- [ ] **Step 5: 통과를 확인한다**

Run: `pnpm vitest run apps/web/src/server`
Expected: PASS

- [ ] **Step 6: 이 검사가 무엇을 잡는지 증명한다**

**(a) 응답 스키마 파싱이 도는가**
`placeOrderAction`의 `placeOrderResultSchema.safeParse(...)`를 지우고 `{ ok: true, data: result.data as PlaceOrderResultDto }`를 돌려준다.
Expected: FAIL — `'응답이 계약 형태가 아니면 실패로 만든다'`가 `ok: true`를 받아 실패한다. **이 회귀는 `undefined` orderId로 주문 상세 페이지로 라우팅한다.**
되돌린다.

**(b) 거절이 성공으로 오는가**
`placeOrderAction`이 `data.status === 'PAYMENT_FAILED'`일 때 `{ ok: false, code: PAYMENT_DECLINED }`를 돌려주게 바꾼다.
Expected: FAIL — `'결제 거절도 성공 응답이다'`가 실패한다. **이 회귀는 결제 거절 시 주문 번호를 화면에 전달하지 못하게 만들고, 사용자가 "다시 시도"할 대상을 잃는다.** 계획 4가 201을 고른 이유가 그것이다.
되돌린다.

- [ ] **Step 7: 전체 검증과 커밋**

```bash
git add apps/web/src/server apps/web/app/api
git commit -m "feat(web): 주문·취소 BFF action과 Route Handler를 추가한다"
```

---

### Task 6: features/sign-in — 훅 TDD의 형태를 세운다

**Files:**
- Create: `apps/web/src/features/sign-in/{model/use-sign-in.ts, ui/SignInForm.tsx, index.ts}`
- Create: `apps/web/src/features/sign-in/model/use-sign-in.spec.ts`
- Create: `apps/web/src/features/sign-in/ui/SignInForm.spec.tsx`
- Create: `apps/web/app/sign-in/page.tsx`
- Modify: `apps/web/src/shared/api/msw/server.ts` (BFF Route Handler용 핸들러 추가)

**Interfaces:**
- Produces:
  - `useSignIn(): { signIn(input: SignInBody): Promise<boolean>; pending: boolean; error: string | null }`
  - `<SignInForm />` — 이메일·비밀번호 폼, 성공 시 `/`로 이동
  - `SUBMIT_PATH = '/api/auth/sign-in'`

**이 태스크가 나머지 다섯 feature의 형태를 정한다.** 일곱 훅이 전부 같은 골격이다 — 요청 보내고, `pending`을 켜고, 실패면 `error` 문구를 담고, 성공하면 호출자에게 알린다. 여기서 그 형태를 정확히 세우고 나머지는 따른다.

**훅이 `/api/...`를 부른다 — Nest를 직접 부르지 않는다.** 변경은 클라이언트 → Route Handler → Nest로 간다(스펙 §8.7). 브라우저가 Nest를 직접 부르면 토큰을 브라우저가 들어야 하고, 그것이 §8.5가 막으려는 것이다.

**MSW 핸들러가 두 종류가 된다.** 지금까지의 핸들러는 `${API_BASE_URL}/...`(Nest)를 가로챘다. 훅 테스트는 **`/api/...`(우리 BFF)**를 가로채야 한다. 두 무리를 파일로 나눈다: `handlers/*.ts`(Nest)와 `handlers/bff.ts`(Route Handler). 섞으면 어느 계층을 테스트하는지 읽는 사람이 알 수 없다.

- [ ] **Step 1: BFF 핸들러를 만든다**

`apps/web/src/shared/api/msw/handlers/bff.ts`:

```ts
import { HttpResponse, http } from 'msw';
import { ORDER_ID, anAddressDto } from '../fixtures';

/**
 * **우리 Route Handler를 가로챈다** — Nest가 아니다. 훅 테스트의 seam이 여기다.
 *
 * 상대 경로를 쓰는 이유: 브라우저(jsdom)에서 훅이 `fetch('/api/...')`를 부르고,
 * MSW가 `location.origin`을 기준으로 해석한다.
 *
 * 응답을 계약 스키마로 파싱하지 않는다 — 이것은 **우리 BFF의 응답 형태**이고
 * `@commerce/contracts`가 정의하지 않는다. 대신 `bff-response.ts`가 그 형태의
 * 단일 출처이고 `bff-response.spec.ts`가 그것을 고정한다.
 */
export const bffHandlers = [
  http.post('/api/auth/sign-in', () => new HttpResponse(null, { status: 204 })),
  http.post('/api/auth/sign-out', () => new HttpResponse(null, { status: 204 })),
  http.post('/api/cart/items', () => new HttpResponse(null, { status: 204 })),
  http.put('/api/cart/items/:skuId', () => new HttpResponse(null, { status: 204 })),
  http.delete('/api/cart/items/:skuId', () => new HttpResponse(null, { status: 204 })),
  http.post('/api/addresses', () => HttpResponse.json(anAddressDto(), { status: 200 })),
  http.post('/api/orders', () =>
    HttpResponse.json({ orderId: ORDER_ID, status: 'PAID' }, { status: 200 }),
  ),
  http.post('/api/orders/:orderId/cancel', () =>
    HttpResponse.json({ status: 'REFUND_PENDING' }, { status: 200 }),
  ),
];
```

`server.ts`에 `...bffHandlers`를 더한다.

- [ ] **Step 2: 훅의 실패하는 테스트를 쓴다**

```ts
import { ErrorCode } from '@commerce/contracts';
import { act, renderHook, waitFor } from '@testing-library/react';
import { HttpResponse, http } from 'msw';
import { describe, expect, it } from 'vitest';
import { MESSAGES } from '@/shared/lib/api-error';
import { server } from '@/shared/api/msw/server';
import { useSignIn } from './use-sign-in';

const CREDENTIALS = { email: 'a@example.com', password: 'correct horse battery staple' };

describe('useSignIn', () => {
  it('처음에는 대기 중도 아니고 에러도 없다', () => {
    const { result } = renderHook(() => useSignIn());

    expect(result.current.pending).toBe(false);
    expect(result.current.error).toBeNull();
  });

  it('성공하면 true를 돌려준다', async () => {
    const { result } = renderHook(() => useSignIn());

    let ok: boolean | undefined;
    await act(async () => {
      ok = await result.current.signIn(CREDENTIALS);
    });

    expect(ok).toBe(true);
    expect(result.current.error).toBeNull();
  });

  it('요청 중에는 pending이 켜지고 끝나면 꺼진다', async () => {
    // 버튼을 두 번 누르는 것을 막는 값이다. 안 꺼지면 화면이 영영 잠긴다.
    const { result } = renderHook(() => useSignIn());

    let resolveRequest: (() => void) | undefined;
    server.use(
      http.post('/api/auth/sign-in', async () => {
        await new Promise<void>((resolve) => {
          resolveRequest = resolve;
        });
        return new HttpResponse(null, { status: 204 });
      }),
    );

    let promise: Promise<boolean> | undefined;
    act(() => {
      promise = result.current.signIn(CREDENTIALS);
    });
    await waitFor(() => expect(result.current.pending).toBe(true));

    await act(async () => {
      resolveRequest?.();
      await promise;
    });

    expect(result.current.pending).toBe(false);
  });

  it('실패하면 false를 돌려주고 코드에 맞는 문구를 담는다', async () => {
    // 스펙 §8.6: 상태 코드가 아니라 code로 분기한다.
    server.use(
      http.post('/api/auth/sign-in', () =>
        HttpResponse.json(
          { code: ErrorCode.INVALID_CREDENTIALS, message: '자격 증명이 올바르지 않습니다.' },
          { status: 401 },
        ),
      ),
    );
    const { result } = renderHook(() => useSignIn());

    let ok: boolean | undefined;
    await act(async () => {
      ok = await result.current.signIn(CREDENTIALS);
    });

    expect(ok).toBe(false);
    expect(result.current.error).toBe(MESSAGES[ErrorCode.INVALID_CREDENTIALS]);
  });

  it('다시 시도하면 이전 에러가 지워진다', async () => {
    // 지우지 않으면 성공한 뒤에도 빨간 문구가 남는다.
    server.use(
      http.post('/api/auth/sign-in', () =>
        HttpResponse.json({ code: ErrorCode.INVALID_CREDENTIALS, message: 'x' }, { status: 401 }),
      ),
    );
    const { result } = renderHook(() => useSignIn());
    await act(async () => {
      await result.current.signIn(CREDENTIALS);
    });
    expect(result.current.error).not.toBeNull();

    server.resetHandlers();
    await act(async () => {
      await result.current.signIn(CREDENTIALS);
    });

    expect(result.current.error).toBeNull();
  });

  it('네트워크가 끊겨도 던지지 않는다', async () => {
    // 훅이 던지면 React 트리가 통째로 죽는다.
    server.use(http.post('/api/auth/sign-in', () => HttpResponse.error()));
    const { result } = renderHook(() => useSignIn());

    let ok: boolean | undefined;
    await act(async () => {
      ok = await result.current.signIn(CREDENTIALS);
    });

    expect(ok).toBe(false);
    expect(result.current.error).toBe(MESSAGES[ErrorCode.INTERNAL_ERROR]);
  });
});
```

- [ ] **Step 3: 실패를 확인한다**

Run: `pnpm vitest run apps/web/src/features/sign-in`
Expected: FAIL — `use-sign-in.ts`가 없다.

- [ ] **Step 4: 훅을 구현한다**

```ts
'use client';

import { ErrorCode, type SignInBody } from '@commerce/contracts';
import { useCallback, useState } from 'react';
import { MESSAGES, readActionResult } from '@/shared/lib/api-error';

export const SIGN_IN_PATH = '/api/auth/sign-in';

/**
 * 다섯 feature 훅이 공유하는 골격: 요청 → `pending` → 실패면 `error` 문구.
 *
 * **던지지 않는다.** 훅이 던지면 React 트리가 통째로 죽는다 — 네트워크 오류까지
 * `error` 상태로 흡수하고 호출자에게는 `boolean`으로 알린다.
 *
 * `error`를 요청 시작 시 지우는 이유: 지우지 않으면 성공한 뒤에도 빨간 문구가 남는다.
 */
export function useSignIn() {
  const [pending, setPending] = useState(false);
  const [error, setError] = useState<string | null>(null);

  const signIn = useCallback(async (input: SignInBody): Promise<boolean> => {
    setPending(true);
    setError(null);
    try {
      const response = await fetch(SIGN_IN_PATH, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(input),
      });
      const result = await readActionResult(response);
      if (!result.ok) {
        setError(result.message);
        return false;
      }
      return true;
    } catch {
      // fetch 자체가 거부된 경우(오프라인, DNS 실패). readActionResult는
      // 응답이 있어야 동작하므로 여기서만 잡을 수 있다.
      setError(MESSAGES[ErrorCode.INTERNAL_ERROR]);
      return false;
    } finally {
      setPending(false);
    }
  }, []);

  return { signIn, pending, error };
}
```

- [ ] **Step 5: 폼과 페이지를 쓴다 (test-after)**

```tsx
// ui/SignInForm.tsx
'use client';

import { useRouter } from 'next/navigation';
import { type FormEvent, useState } from 'react';
import { useSignIn } from '../model/use-sign-in';

export function SignInForm() {
  const router = useRouter();
  const { signIn, pending, error } = useSignIn();
  const [email, setEmail] = useState('');
  const [password, setPassword] = useState('');

  async function handleSubmit(event: FormEvent): Promise<void> {
    event.preventDefault();
    if (await signIn({ email, password })) {
      // refresh가 있어야 Header가 로그인 상태를 다시 읽는다 — RSC 캐시를 비운다.
      router.push('/');
      router.refresh();
    }
  }

  return (
    <form onSubmit={handleSubmit}>
      <h1>로그인</h1>
      <label>
        이메일
        <input
          type="email"
          name="email"
          required
          value={email}
          onChange={(event) => setEmail(event.target.value)}
        />
      </label>
      <label>
        비밀번호
        <input
          type="password"
          name="password"
          required
          value={password}
          onChange={(event) => setPassword(event.target.value)}
        />
      </label>
      {error === null ? null : <p role="alert">{error}</p>}
      <button type="submit" disabled={pending}>
        {pending ? '로그인 중…' : '로그인'}
      </button>
    </form>
  );
}
```

`app/sign-in/page.tsx`는 `<SignInForm />` 한 줄이다.

`SignInForm.spec.tsx`가 덮는 것(test-after, 행동만):
- 이메일·비밀번호를 입력하고 제출하면 요청이 나간다
- 실패하면 `role="alert"`에 문구가 보인다
- 요청 중에는 버튼이 `disabled`다

**`useRouter`를 어떻게 다루는가.** `next/navigation`의 `useRouter`는 App Router 컨텍스트를 요구한다. 목 라이브러리는 금지이므로 **폼에서 라우팅을 분리한다**: `SignInForm`이 `onSuccess?: () => void` prop을 받고, 라우팅은 `app/sign-in/page.tsx`(클라이언트 컴포넌트)가 한다. 그러면 폼 테스트가 라우터 없이 돈다.

```tsx
export function SignInForm({ onSuccess }: { onSuccess?: () => void }) {
  // ... 성공 시 onSuccess?.();
}
```

```tsx
// app/sign-in/page.tsx
'use client';
import { useRouter } from 'next/navigation';
import { SignInForm } from '@/features/sign-in';

export default function SignInPage() {
  const router = useRouter();
  return (
    <SignInForm
      onSuccess={() => {
        router.push('/');
        router.refresh();
      }}
    />
  );
}
```

**이 분리는 태스크 7~10의 모든 feature UI에 적용한다.** 라우팅·새로고침은 `app/`이 하고 feature는 결과만 알린다 — 편차 1이 만든 구조와 같은 방향이다.

- [ ] **Step 6: 통과를 확인한다**

Run: `pnpm vitest run apps/web/src/features/sign-in`
Expected: PASS

- [ ] **Step 7: 이 검사가 무엇을 잡는지 증명한다**

**(a) `pending`이 꺼지는가**
`finally { setPending(false); }`를 `try` 블록 끝으로 옮긴다(실패 경로에서 안 꺼짐).
Expected: FAIL — 실패 케이스 뒤에 `pending`이 `true`로 남는다. 단, 현재 spec이 실패 후 `pending`을 단언하지 않으므로 **통과할 수도 있다.** 통과하면 `'실패하면 false를 돌려주고…'`에 `expect(result.current.pending).toBe(false)`를 더해 판별력을 만든다. 관측과 조치를 보고서에 적는다.
되돌린다.

**(b) 에러 초기화가 있는가**
`setError(null)`을 지운다.
Expected: FAIL — `'다시 시도하면 이전 에러가 지워진다'`가 실패한다.
되돌린다.

**(c) 네트워크 오류를 흡수하는가**
`try`/`catch`를 걷어낸다.
Expected: FAIL — `'네트워크가 끊겨도 던지지 않는다'`가 실패한다. **이 회귀는 오프라인 상태에서 화면 전체를 흰 화면으로 만든다.**
되돌린다.

- [ ] **Step 8: 전체 검증과 커밋**

Run: `pnpm verify`
Expected: exit 0. `arch:check`가 통과한다 — features는 widgets·views를 import하지 않고 `src/server`도 부르지 않는다.

```bash
git add apps/web/src/features apps/web/src/shared/api apps/web/app/sign-in
git commit -m "feat(web): 로그인 feature와 훅 테스트 형태를 세운다"
```

---

### Task 7: features/cart-items · manage-addresses

**Files:**
- Create: `apps/web/src/features/cart-items/model/{use-add-to-cart.ts, use-remove-from-cart.ts}` + 각각 spec
- Create: `apps/web/src/features/cart-items/ui/{AddToCartButton.tsx, RemoveFromCartButton.tsx}` + 각각 spec
- Create: `apps/web/src/features/cart-items/index.ts`
- Create: `apps/web/src/features/manage-addresses/{model/use-add-address.ts, ui/AddressPicker.tsx, index.ts}` + 각각 spec

**슬라이스 이름이 `add-to-cart`가 아니라 `cart-items`인 이유.** 장바구니 줄을 담고·빼는 것은 **하나의 사용자 관심사**이고, FSD의 슬라이스는 그 단위로 자른다. `add-to-cart`와 `remove-from-cart`를 슬라이스 둘로 나누면 둘 다 `/api/cart/items`를 부르는데 슬라이스 간에는 `index.ts`로만 참조할 수 있어 공통 코드를 둘 곳이 없어진다. 수량 변경(`PUT`)은 화면이 아직 쓰지 않으므로 만들지 않는다 — 필요해지면 이 슬라이스에 더한다.

**Interfaces:**
- Consumes: 태스크 6이 세운 훅 골격, `readActionResult`, `MESSAGES`, `entities/address`의 `AddressLine`
- Produces:
  - `useAddToCart(): { addToCart(skuId: string, quantity: number): Promise<boolean>; pending; error }`
  - `useRemoveFromCart(): { removeFromCart(skuId: string): Promise<boolean>; pending; error }`
  - `<AddToCartButton skuId onAdded?={() => void} />` — 수량 입력 + 담기
  - `<RemoveFromCartButton skuId onRemoved?={() => void} />`
  - `useAddAddress(): { addAddress(input: AddressBody): Promise<AddressDto | null>; pending; error }`
  - `<AddressPicker addresses selectedId onSelect onAdded? />` — 목록에서 고르거나 새로 추가

**둘을 한 태스크로 묶는 이유.** 둘 다 태스크 6이 세운 훅 골격을 그대로 따르는 얇은 슬라이스이고, 리뷰어가 하나만 반려할 상황이 없다. **`manage-addresses`는 "추가 + 선택"만 만든다** — 수정·삭제·기본 배송지 변경 UI는 백로그다(범위 절 참조).

**`AddToCartButton`이 수량 입력을 갖는 이유.** 상품 상세에서 "2개 담기"가 가능해야 E2E의 재고 부족 시나리오(재고 1개인데 3개 담기)를 만들 수 있다. 수량 없이 항상 1개면 그 시나리오는 API를 직접 불러야 하고, 그러면 브라우저 E2E가 아니게 된다.

- [ ] **Step 1: `useAddToCart`의 실패하는 테스트를 쓴다**

태스크 6의 `use-sign-in.spec.ts`와 같은 여섯 케이스를 덮되, 도메인 케이스를 하나 더한다.

```ts
  it('재고가 부족하면 그 문구를 담는다', async () => {
    // 담기 단계에서는 재고를 보지 않지만(예약은 주문 시점이다), API가 그 코드를
    // 낼 수 있는 계약이므로 화면이 분기할 수 있어야 한다.
    server.use(
      http.post('/api/cart/items', () =>
        HttpResponse.json({ code: ErrorCode.INSUFFICIENT_STOCK, message: 'x' }, { status: 409 }),
      ),
    );
    const { result } = renderHook(() => useAddToCart());

    await act(async () => {
      await result.current.addToCart(SKU_ID, 3);
    });

    expect(result.current.error).toBe(MESSAGES[ErrorCode.INSUFFICIENT_STOCK]);
  });

  it('본문에 skuId와 quantity를 싣는다', async () => {
    // 계약을 벗어난 본문을 보내면 MSW의 Nest 핸들러가 던지지만, BFF 핸들러는
    // 검사하지 않는다 — 그래서 이 단언이 필요하다.
    let seen: unknown = null;
    server.use(
      http.post('/api/cart/items', async ({ request }) => {
        seen = await request.json();
        return new HttpResponse(null, { status: 204 });
      }),
    );
    const { result } = renderHook(() => useAddToCart());

    await act(async () => {
      await result.current.addToCart(SKU_ID, 3);
    });

    expect(seen).toEqual({ skuId: SKU_ID, quantity: 3 });
  });
```

- [ ] **Step 2: 실패를 확인하고 구현한다**

Run: `pnpm vitest run apps/web/src/features/cart-items`
Expected: FAIL → 구현 → PASS

```ts
'use client';

import { ErrorCode } from '@commerce/contracts';
import { useCallback, useState } from 'react';
import { MESSAGES, readActionResult } from '@/shared/lib/api-error';

export function useAddToCart() {
  const [pending, setPending] = useState(false);
  const [error, setError] = useState<string | null>(null);

  const addToCart = useCallback(async (skuId: string, quantity: number): Promise<boolean> => {
    setPending(true);
    setError(null);
    try {
      const response = await fetch('/api/cart/items', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ skuId, quantity }),
      });
      const result = await readActionResult(response);
      if (!result.ok) {
        setError(result.message);
        return false;
      }
      return true;
    } catch {
      setError(MESSAGES[ErrorCode.INTERNAL_ERROR]);
      return false;
    } finally {
      setPending(false);
    }
  }, []);

  return { addToCart, pending, error };
}
```

```tsx
// ui/AddToCartButton.tsx
'use client';

import { useState } from 'react';
import { useAddToCart } from '../model/use-add-to-cart';

/**
 * 라우팅·새로고침을 하지 않는다 — `onAdded`로 알리고 `app/`이 결정한다.
 * 태스크 6이 `SignInForm`에 세운 규칙과 같다.
 */
export function AddToCartButton({ skuId, onAdded }: { skuId: string; onAdded?: () => void }) {
  const { addToCart, pending, error } = useAddToCart();
  const [quantity, setQuantity] = useState(1);

  return (
    <div>
      <label>
        수량
        <input
          type="number"
          min={1}
          value={quantity}
          onChange={(event) => setQuantity(Number(event.target.value))}
        />
      </label>
      <button
        type="button"
        disabled={pending}
        onClick={async () => {
          if (await addToCart(skuId, quantity)) {
            onAdded?.();
          }
        }}
      >
        {pending ? '담는 중…' : '장바구니에 담기'}
      </button>
      {error === null ? null : <p role="alert">{error}</p>}
    </div>
  );
}
```

`AddToCartButton.spec.tsx`(test-after): 수량을 3으로 바꾸고 누르면 그 값이 요청에 실린다 / 실패하면 `role="alert"`이 보인다 / 성공하면 `onAdded`가 불린다 / 요청 중에는 버튼이 `disabled`다.

`useRemoveFromCart`와 `RemoveFromCartButton`은 같은 골격이고 `DELETE /api/cart/items/{skuId}`를 부른다. 훅 spec은 (1) 성공 시 `true`, (2) 없는 줄이면 `NOT_FOUND` 문구, (3) `pending` 전이를 덮는다.

- [ ] **Step 3: `useAddAddress`와 `AddressPicker`**

훅은 같은 골격이되 **성공 시 `AddressDto`를 돌려준다** — 새로 만든 주소를 바로 선택해야 하기 때문이다.

```ts
  const addAddress = useCallback(async (input: AddressBody): Promise<AddressDto | null> => {
    setPending(true);
    setError(null);
    try {
      const response = await fetch('/api/addresses', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(input),
      });
      const result = await readActionResult<unknown>(response, (body) => body);
      if (!result.ok) {
        setError(result.message);
        return null;
      }
      const parsed = addressDtoSchema.safeParse(result.data);
      if (!parsed.success) {
        setError(MESSAGES[ErrorCode.INTERNAL_ERROR]);
        return null;
      }
      return parsed.data;
    } catch {
      setError(MESSAGES[ErrorCode.INTERNAL_ERROR]);
      return null;
    } finally {
      setPending(false);
    }
  }, []);
```

훅 spec에 더할 케이스: **응답이 계약 형태가 아니면 `null`이고 `INTERNAL_ERROR` 문구다** — 태스크 5의 `placeOrderAction`과 같은 이유다.

`AddressPicker`는 라디오 목록(`entities/address`의 `AddressLine`으로 각 줄을 그림)과 "새 배송지 추가" 폼을 갖는다. 주소가 하나도 없으면 폼만 보여준다.

**라벨 문자열이 E2E와의 계약이다** — `받는 사람`·`연락처`·`우편번호`·`주소`·`배송지 추가`. 태스크 14의 `readyToOrder`가 그 라벨로 폼을 채운다.

```tsx
'use client';

import type { AddressBody, AddressDto } from '@commerce/contracts';
import { type FormEvent, useState } from 'react';
import { AddressLine } from '@/entities/address';
import { useAddAddress } from '../model/use-add-address';

const EMPTY: AddressBody = {
  label: '기본 배송지',
  recipient: '',
  phone: '',
  zip: '',
  line1: '',
};

/**
 * `label`을 폼에서 받지 않는 이유: 계약이 요구하는 필드지만 사용자에게 물을 가치가
 * 없다. 주소록 관리 화면이 생기면 그때 편집 가능하게 만든다(범위 절).
 */
export function AddressPicker({
  addresses,
  selectedId,
  onSelect,
  onAdded,
}: {
  addresses: AddressDto[];
  selectedId: string | null;
  onSelect: (addressId: string) => void;
  onAdded?: () => void;
}) {
  const { addAddress, pending, error } = useAddAddress();
  const [draft, setDraft] = useState<AddressBody>(EMPTY);

  async function handleSubmit(event: FormEvent): Promise<void> {
    event.preventDefault();
    const created = await addAddress(draft);
    if (created === null) {
      return;
    }
    // 방금 만든 주소를 바로 고른다 — 안 그러면 사용자가 주문 버튼을 누를 수 없다.
    onSelect(created.id);
    setDraft(EMPTY);
    onAdded?.();
  }

  const field = (key: keyof Omit<AddressBody, 'label' | 'line2'>, label: string) => (
    <label key={key}>
      {label}
      <input
        name={key}
        required
        value={draft[key]}
        onChange={(event) => setDraft({ ...draft, [key]: event.target.value })}
      />
    </label>
  );

  return (
    <section>
      {addresses.length === 0 ? (
        <p>배송지를 추가해 주세요.</p>
      ) : (
        <ul role="list">
          {addresses.map((address) => (
            <li key={address.id}>
              <label>
                <input
                  type="radio"
                  name="addressId"
                  value={address.id}
                  checked={selectedId === address.id}
                  onChange={() => onSelect(address.id)}
                />
                <AddressLine address={address} />
              </label>
            </li>
          ))}
        </ul>
      )}

      <form onSubmit={handleSubmit}>
        {field('recipient', '받는 사람')}
        {field('phone', '연락처')}
        {field('zip', '우편번호')}
        {field('line1', '주소')}
        {error === null ? null : <p role="alert">{error}</p>}
        <button type="submit" disabled={pending}>
          {pending ? '추가 중…' : '배송지 추가'}
        </button>
      </form>
    </section>
  );
}
```

`AddressPicker.spec.tsx`가 덮는 것: 주소 목록이 라디오로 보인다 / 고르면 `onSelect`가 그 id로 불린다 / 주소가 없으면 "배송지를 추가해 주세요" 안내와 폼만 보인다 / 폼을 제출하면 새 주소가 만들어지고 `onSelect`와 `onAdded`가 불린다.

- [ ] **Step 4: 이 검사가 무엇을 잡는지 증명한다**

**(a) 수량이 실제로 실리는가**
`AddToCartButton`의 `addToCart(skuId, quantity)`를 `addToCart(skuId, 1)`로 바꾼다.
Expected: FAIL — `'수량을 3으로 바꾸고 누르면 그 값이 요청에 실린다'`가 실패한다. **이 회귀는 E2E의 재고 부족 시나리오를 통째로 무력화한다** — 항상 1개만 담기므로 재고 1개에서 부족이 나지 않는다.
되돌린다.

**(b) 새 주소가 선택되는가**
`AddressPicker`의 폼 제출 성공 시 `onSelect(created.id)` 호출을 지운다.
Expected: FAIL — `'폼을 제출하면 새 주소가 만들어지고 onSelect가 불린다'`가 실패한다. **이 회귀는 주소를 막 추가한 사용자가 주문 버튼을 누를 수 없게 만든다**(선택된 배송지가 없으므로).
되돌린다.

**(c) 응답 스키마 파싱이 도는가**
`useAddAddress`의 `addressDtoSchema.safeParse(...)`를 지우고 `result.data as AddressDto`를 돌려준다.
Expected: FAIL — `'응답이 계약 형태가 아니면 null이다'`가 실패한다.
되돌린다.

- [ ] **Step 5: 전체 검증과 커밋**

```bash
git add apps/web/src/features
git commit -m "feat(web): 장바구니 줄 조작과 배송지 선택 feature를 추가한다"
```

---

### Task 8: features/place-order · cancel-order

**Files:**
- Create: `apps/web/src/features/place-order/{model/use-place-order.ts, ui/PlaceOrderButton.tsx, index.ts}` + 각각 spec
- Create: `apps/web/src/features/cancel-order/{model/use-cancel-order.ts, ui/CancelOrderButton.tsx, index.ts}` + 각각 spec

**Interfaces:**
- Produces:
  - `usePlaceOrder(): { placeOrder(addressId: string): Promise<PlaceOrderResultDto | null>; pending; error }`
  - `<PlaceOrderButton addressId onPlaced={(result) => void} />`
  - `useCancelOrder(): { cancelOrder(orderId: string): Promise<CancelOrderResultDto | null>; pending; error }`
  - `<CancelOrderButton orderId onCancelled={(result) => void} />`

**이 둘이 사가의 사용자 쪽 표면이다.** `placeOrder`가 돌려주는 `status`로 화면이 "주문이 완료되었습니다"와 "결제가 거절되었습니다"를 가르고, `cancelOrder`의 `status`가 "취소되었습니다"와 "환불 처리 중입니다"를 가른다.

**`onPlaced`가 결과 전체를 받는 이유.** 성공/실패만으로는 부족하다 — 호출자가 `orderId`로 라우팅하고 `status`로 문구를 골라야 한다. 태스크 6이 `boolean`을 쓴 것과 다른 이유가 이것이다.

- [ ] **Step 1: `usePlaceOrder`의 실패하는 테스트를 쓴다**

```ts
describe('usePlaceOrder', () => {
  it('성공하면 orderId와 status를 돌려준다', async () => {
    const { result } = renderHook(() => usePlaceOrder());

    let placed: PlaceOrderResultDto | null | undefined;
    await act(async () => {
      placed = await result.current.placeOrder(ADDRESS_ID);
    });

    expect(placed).toEqual({ orderId: ORDER_ID, status: 'PAID' });
  });

  it('결제 거절도 성공이고 status가 PAYMENT_FAILED다', async () => {
    // 계획 4의 결정: 주문은 만들어졌고 번호가 있다. 화면이 status로 분기한다.
    server.use(
      http.post('/api/orders', () =>
        HttpResponse.json({ orderId: ORDER_ID, status: 'PAYMENT_FAILED' }, { status: 200 }),
      ),
    );
    const { result } = renderHook(() => usePlaceOrder());

    let placed: PlaceOrderResultDto | null | undefined;
    await act(async () => {
      placed = await result.current.placeOrder(ADDRESS_ID);
    });

    expect(placed?.status).toBe('PAYMENT_FAILED');
    // 거절은 오류가 아니다 — 빨간 문구를 띄우지 않는다.
    expect(result.current.error).toBeNull();
  });

  it('재고가 부족하면 null이고 그 문구를 담는다', async () => {
    server.use(
      http.post('/api/orders', () =>
        HttpResponse.json({ code: ErrorCode.INSUFFICIENT_STOCK, message: 'x' }, { status: 409 }),
      ),
    );
    const { result } = renderHook(() => usePlaceOrder());

    let placed: PlaceOrderResultDto | null | undefined;
    await act(async () => {
      placed = await result.current.placeOrder(ADDRESS_ID);
    });

    expect(placed).toBeNull();
    expect(result.current.error).toBe(MESSAGES[ErrorCode.INSUFFICIENT_STOCK]);
  });

  it('빈 장바구니면 그 문구를 담는다', async () => {
    server.use(
      http.post('/api/orders', () =>
        HttpResponse.json({ code: ErrorCode.DOMAIN_RULE_VIOLATED, message: 'x' }, { status: 422 }),
      ),
    );
    const { result } = renderHook(() => usePlaceOrder());

    await act(async () => {
      await result.current.placeOrder(ADDRESS_ID);
    });

    expect(result.current.error).toBe(MESSAGES[ErrorCode.DOMAIN_RULE_VIOLATED]);
  });

  it('응답이 계약 형태가 아니면 null이다', async () => {
    server.use(http.post('/api/orders', () => HttpResponse.json({ weird: true }, { status: 200 })));
    const { result } = renderHook(() => usePlaceOrder());

    let placed: PlaceOrderResultDto | null | undefined;
    await act(async () => {
      placed = await result.current.placeOrder(ADDRESS_ID);
    });

    expect(placed).toBeNull();
    expect(result.current.error).toBe(MESSAGES[ErrorCode.INTERNAL_ERROR]);
  });
});
```

`useCancelOrder`도 같은 형태이고, 도메인 케이스는 **`ORDER_NOT_CANCELLABLE`(409)과 `FORBIDDEN`(403)**이다.

- [ ] **Step 2: 실패를 확인하고 구현한다**

Run: `pnpm vitest run apps/web/src/features/place-order apps/web/src/features/cancel-order`
Expected: FAIL → 구현 → PASS

훅은 태스크 7의 `useAddAddress`와 같은 골격이다 — `readActionResult<unknown>` → 실패면 `null` + 문구, 성공이면 `placeOrderResultSchema`/`cancelOrderResultSchema`로 파싱.

- [ ] **Step 3: 버튼을 쓴다 (test-after)**

```tsx
// ui/PlaceOrderButton.tsx
'use client';

import type { PlaceOrderResultDto } from '@commerce/contracts';
import { usePlaceOrder } from '../model/use-place-order';

/**
 * `addressId`가 없으면 버튼이 비활성이다 — 배송지를 고르지 않고 주문할 수 없다.
 * 서버도 400으로 막지만, 누를 수 있게 두면 사용자가 왜 실패했는지 모른다.
 */
export function PlaceOrderButton({
  addressId,
  onPlaced,
}: {
  addressId: string | null;
  onPlaced: (result: PlaceOrderResultDto) => void;
}) {
  const { placeOrder, pending, error } = usePlaceOrder();

  return (
    <div>
      <button
        type="button"
        disabled={pending || addressId === null}
        onClick={async () => {
          if (addressId === null) {
            return;
          }
          const result = await placeOrder(addressId);
          if (result !== null) {
            onPlaced(result);
          }
        }}
      >
        {pending ? '주문 중…' : '주문하기'}
      </button>
      {error === null ? null : <p role="alert">{error}</p>}
    </div>
  );
}
```

`PlaceOrderButton.spec.tsx`: `addressId`가 `null`이면 버튼이 `disabled`다 / 누르면 `onPlaced`가 결과와 함께 불린다 / **거절이어도 `onPlaced`가 불린다**(주문 번호가 있으므로) / 재고 부족이면 `onPlaced`가 불리지 않고 `role="alert"`이 보인다.

`CancelOrderButton`은 `<button>주문 취소</button>` 하나에 확인 절차 없이 간다 — 확인 다이얼로그는 `window.confirm`을 쓰게 되고 그것은 테스트에서 목이 필요하다(금지). 취소는 되돌릴 수 있는 작업(환불)이므로 확인 없이 진행해도 위험이 낮다. 이 판단을 컴포넌트 주석에 적는다.

- [ ] **Step 4: 이 검사가 무엇을 잡는지 증명한다**

**(a) 거절이 오류로 취급되지 않는가**
`usePlaceOrder`가 `data.status === 'PAYMENT_FAILED'`일 때 `setError(...)`하고 `null`을 돌려주게 바꾼다.
Expected: FAIL — `'결제 거절도 성공이고 status가 PAYMENT_FAILED다'`가 실패한다. **이 회귀는 결제가 거절된 사용자에게 주문 번호를 주지 못한다.**
되돌린다.

**(b) 배송지 없이 주문을 막는가**
`PlaceOrderButton`의 `disabled={pending || addressId === null}`에서 `addressId === null`을 뺀다.
Expected: FAIL — `'addressId가 null이면 버튼이 disabled다'`가 실패한다.
되돌린다.

- [ ] **Step 5: 전체 검증과 커밋**

```bash
git add apps/web/src/features
git commit -m "feat(web): 주문·취소 feature를 추가한다"
```

---

### Task 9: widgets 셋 — header · product-grid · order-summary

**Files:**
- Create: `apps/web/src/widgets/header/{ui/Header.tsx, index.ts}` + spec
- Create: `apps/web/src/widgets/product-grid/{ui/ProductGrid.tsx, index.ts}` + spec
- Create: `apps/web/src/widgets/order-summary/{ui/OrderSummary.tsx, index.ts}` + spec
- Create: `apps/web/src/features/sign-out/{model/use-sign-out.ts, ui/SignOutButton.tsx, index.ts}` + 각각 spec
- Modify: `apps/web/app/layout.tsx`

**Interfaces:**
- Consumes: `entities/product`의 `ProductCard`, `entities/cart`의 `CartLineRow`, `entities/order`의 `OrderStatusBadge`, `features/sign-out`
- Produces:
  - `<Header signedIn={boolean} />` — 홈·장바구니 링크, 로그인/로그아웃
  - `<ProductGrid products={ProductDto[]} />` — 카드 목록, 비어 있으면 안내
  - `<OrderSummary order={OrderDto} action?={ReactNode} />` — 상태·배송지·라인·총액

**`features/sign-out`이 여기서 생기는 이유.** `Header`가 그것을 쓴다. widget이 feature를 쓰는 것은 FSD 방향(아래로)에 맞고, 로그아웃 훅은 태스크 6의 골격을 그대로 따르는 열 줄이라 별도 태스크가 아깝다.

**`Header`가 `signedIn`을 prop으로 받는 이유.** 세션은 `src/server/`에만 있고 FSD는 그것을 import할 수 없다(`no-server-code-in-fsd`). `app/layout.tsx`가 쿠키를 읽어 넘긴다 — 편차 1이 만든 구조와 같다.

**`OrderSummary`가 `action` 슬롯을 갖는 이유.** 주문 상세는 취소 버튼을 넣고, 향후 다른 화면은 넣지 않는다. 슬롯이 없으면 widget이 `cancel-order` feature를 직접 알게 되고, 그러면 이 widget을 취소 불가능한 맥락에서 쓸 수 없다.

- [ ] **Step 1: `useSignOut`을 만든다 (TDD)**

태스크 6의 `useSignIn`과 같은 골격. `POST /api/auth/sign-out` → 성공하면 `true`.

spec은 (1) 성공 시 `true`, (2) `pending` 전이, (3) 실패해도 던지지 않고 `false`를 덮는다.

**로그아웃 실패에는 문구를 띄우지 않는다** — 실패해도 사용자를 로그인 화면으로 보내는 것이 맞다(쿠키가 이미 지워졌을 수 있고, 남아 있어도 다음 요청이 401을 받는다). `SignOutButton`이 성공 여부와 무관하게 `onSignedOut`을 부르고, 그 판단을 훅 주석에 적는다.

- [ ] **Step 2: widget 셋을 구현한다 (test-after)**

```tsx
// widgets/header/ui/Header.tsx
import Link from 'next/link';
import { SignOutButton } from '@/features/sign-out';

/**
 * `signedIn`을 prop으로 받는다 — 세션은 `src/server/`에만 있고 FSD 레이어는
 * 그것을 import할 수 없다(`no-server-code-in-fsd`). `app/layout.tsx`가 쿠키를
 * 읽어 넘긴다.
 */
export function Header({ signedIn, onSignedOut }: { signedIn: boolean; onSignedOut?: () => void }) {
  return (
    <header>
      <nav>
        <Link href="/">상품</Link>
        <Link href="/cart">장바구니</Link>
        {signedIn ? (
          <SignOutButton onSignedOut={onSignedOut} />
        ) : (
          <Link href="/sign-in">로그인</Link>
        )}
      </nav>
    </header>
  );
}
```

```tsx
// widgets/product-grid/ui/ProductGrid.tsx
import type { ProductDto } from '@commerce/contracts';
import { ProductCard } from '@/entities/product';

export function ProductGrid({ products }: { products: ProductDto[] }) {
  if (products.length === 0) {
    return <p>판매 중인 상품이 없습니다.</p>;
  }
  return (
    <ul role="list">
      {products.map((product) => (
        <li key={product.id}>
          <ProductCard product={product} />
        </li>
      ))}
    </ul>
  );
}
```

```tsx
// widgets/order-summary/ui/OrderSummary.tsx
import type { OrderDto } from '@commerce/contracts';
import type { ReactNode } from 'react';
import { CartLineRow } from '@/entities/cart';
import { OrderStatusBadge } from '@/entities/order';
import { formatMoney } from '@/shared/lib/format-money';

/**
 * 주문 라인을 `CartLineRow`로 그리는 이유: 두 DTO의 표시 필드가 같고(이름·단가·수량·소계),
 * 표기를 두 벌 두면 장바구니와 주문서의 금액 표기가 갈라진다.
 */
export function OrderSummary({ order, action }: { order: OrderDto; action?: ReactNode }) {
  return (
    <section>
      <h1>주문 {order.id}</h1>
      <p>
        상태: <OrderStatusBadge status={order.status} />
      </p>
      <h2>배송지</h2>
      <p>
        {order.shippingAddress.recipient} · {order.shippingAddress.phone}
        <br />[{order.shippingAddress.zip}] {order.shippingAddress.line1}
        {order.shippingAddress.line2 === null ? '' : ` ${order.shippingAddress.line2}`}
      </p>
      <h2>주문 상품</h2>
      <table>
        <thead>
          <tr>
            <th>상품</th>
            <th>단가</th>
            <th>수량</th>
            <th>소계</th>
          </tr>
        </thead>
        <tbody>
          {order.lines.map((line) => (
            <CartLineRow key={line.skuId} line={line} />
          ))}
        </tbody>
      </table>
      <p>총 {formatMoney(order.total)}</p>
      {action}
    </section>
  );
}
```

- [ ] **Step 3: `layout.tsx`를 배선한다**

```tsx
import { cookieTokenStore } from '@/server/session';
import { Header } from '@/widgets/header';
import './globals.css';

export const metadata = { title: 'Commerce' };

/**
 * `app/`이 세션을 읽어 `Header`에 넘긴다. FSD 레이어는 `src/server/`를 볼 수 없으므로
 * 이 3줄이 그 경계를 넘는 유일한 지점이다(편차 1).
 */
export default async function RootLayout({ children }: { children: React.ReactNode }) {
  const signedIn = (await (await cookieTokenStore()).read()) !== null;

  return (
    <html lang="ko">
      <body>
        <Header signedIn={signedIn} />
        <main>{children}</main>
      </body>
    </html>
  );
}
```

**`onSignedOut`을 여기서 넘길 수 없다** — `layout.tsx`는 서버 컴포넌트라 함수를 prop으로 내려보낼 수 없다. `SignOutButton`이 자체적으로 `router.refresh()`를 부르게 하고, `Header`의 `onSignedOut` prop은 **넣지 않는다.** 그 예외를 `SignOutButton` 주석에 적는다 — feature가 라우팅을 하는 유일한 자리이고, 대안(레이아웃을 클라이언트 컴포넌트로 만들기)은 전체 트리를 클라이언트로 만든다.

- [ ] **Step 4: widget spec을 쓴다 (test-after)**

- `Header.spec.tsx`: `signedIn`이 `false`면 "로그인" 링크가 보이고 로그아웃 버튼이 없다 / `true`면 반대다 / 상품·장바구니 링크가 항상 있다
- `ProductGrid.spec.tsx`: 상품 수만큼 항목이 보인다 / 빈 배열이면 안내 문구가 보이고 목록이 없다
- `OrderSummary.spec.tsx`: 상태 배지·배송지·라인·총액이 보인다 / `action`을 주면 그 노드가 렌더되고 안 주면 없다 / `line2`가 `null`이면 빈 문자열이 아니라 아예 안 나온다

- [ ] **Step 5: 이 검사가 무엇을 잡는지 증명한다**

**(a) 로그인 상태 분기가 있는가**
`Header`의 `{signedIn ? ... : ...}`를 항상 로그인 링크를 그리게 바꾼다.
Expected: FAIL — `'signedIn이 true면 로그아웃 버튼이 보인다'`가 실패한다.
되돌린다.

**(b) 빈 목록 안내가 있는가**
`ProductGrid`의 `if (products.length === 0)` 분기를 지운다.
Expected: FAIL — `'빈 배열이면 안내 문구가 보인다'`가 실패한다. **이 회귀는 상품이 없을 때 빈 화면을 보여준다** — 사용자는 로딩 중인지 없는 것인지 알 수 없다.
되돌린다.

**(c) `action` 슬롯이 선택적인가**
`OrderSummary`의 `{action}`을 `<div>{action}</div>`로 감싼다.
Expected: **통과한다.** `action`이 `undefined`면 빈 `div`가 렌더될 뿐 테스트가 보는 것(그 노드의 존재)은 그대로다. 관측 결과를 보고서에 적고, `'action을 안 주면 없다'` 케이스가 실제로는 **자식 노드의 부재가 아니라 특정 텍스트의 부재**를 보고 있다는 것을 기록한다.
되돌린다.

- [ ] **Step 6: 전체 검증과 커밋**

Run: `pnpm verify`
Expected: exit 0. `arch:check`가 통과한다 — widgets는 views를 import하지 않고, entities와 features만 쓴다.

```bash
git add apps/web/src apps/web/app/layout.tsx
git commit -m "feat(web): 헤더·상품 목록·주문 요약 위젯을 추가한다"
```

---

### Task 10: views 넷과 `app/` 라우트 배선

**Files:**
- Create: `apps/web/src/views/product-list/{ui/ProductListView.tsx, index.ts}` + spec
- Create: `apps/web/src/views/product-detail/{ui/ProductDetailView.tsx, index.ts}` + spec
- Create: `apps/web/src/views/cart/{ui/CartView.tsx, index.ts}` + spec
- Create: `apps/web/src/views/order-detail/{ui/OrderDetailView.tsx, index.ts}` + spec
- Modify: `apps/web/app/page.tsx`
- Create: `apps/web/app/products/[productId]/page.tsx`
- Create: `apps/web/app/cart/page.tsx`
- Create: `apps/web/app/orders/[orderId]/page.tsx`

**Interfaces:**
- Consumes: 태스크 3·7·8·9의 전부
- Produces:
  - `<ProductListView products />`
  - `<ProductDetailView product onAdded? />`
  - `<CartView cart addresses onPlaced />`
  - `<OrderDetailView order onCancelled />`

**편차 1이 여기서 형태를 갖는다.** `views`는 데이터를 가져오지 않는다 — `app/`의 RSC가 `src/server/api-client`로 페치하고 props로 넘긴다. 결과적으로 `views`가 순수 컴포넌트라 Testing Library로 테스트할 수 있고, E2E가 덮어야 하는 것은 `app/`의 페치 3줄뿐이다.

**`CartView`가 상태를 갖는 유일한 view다** — 선택된 배송지 id. 그것을 `app/cart/page.tsx`로 올리면 그 페이지가 클라이언트 컴포넌트가 되고, 그러면 페치를 못 한다. `CartView`가 `'use client'`이고 페이지는 서버로 남는다.

- [ ] **Step 1: view 넷을 구현한다**

```tsx
// views/product-list/ui/ProductListView.tsx
import type { ProductDto } from '@commerce/contracts';
import { ProductGrid } from '@/widgets/product-grid';

export function ProductListView({ products }: { products: ProductDto[] }) {
  return (
    <>
      <h1>상품</h1>
      <ProductGrid products={products} />
    </>
  );
}
```

```tsx
// views/product-detail/ui/ProductDetailView.tsx
'use client';

import type { ProductDto } from '@commerce/contracts';
import { AddToCartButton } from '@/features/cart-items';
import { formatMoney } from '@/shared/lib/format-money';

/**
 * SKU마다 담기 버튼을 둔다. 옵션 선택 UI(드롭다운)를 만들지 않는 이유: SKU가
 * 코드 문자열 하나뿐이라 고를 축이 없다. 옵션 축(색·사이즈)이 생기면 그때 만든다.
 */
export function ProductDetailView({
  product,
  onAdded,
}: {
  product: ProductDto;
  onAdded?: () => void;
}) {
  return (
    <>
      <h1>{product.name}</h1>
      <ul role="list">
        {product.skus.map((sku) => (
          <li key={sku.id}>
            <h2>{sku.code}</h2>
            <p>{formatMoney(sku.price)}</p>
            <AddToCartButton skuId={sku.id} onAdded={onAdded} />
          </li>
        ))}
      </ul>
    </>
  );
}
```

```tsx
// views/cart/ui/CartView.tsx
'use client';

import type { AddressDto, CartDto, PlaceOrderResultDto } from '@commerce/contracts';
import { useState } from 'react';
import { CartLineRow } from '@/entities/cart';
import { RemoveFromCartButton } from '@/features/cart-items';
import { AddressPicker } from '@/features/manage-addresses';
import { PlaceOrderButton } from '@/features/place-order';
import { formatMoney } from '@/shared/lib/format-money';

/**
 * 선택된 배송지 id를 여기서 들고 있다 — `app/cart/page.tsx`로 올리면 그 페이지가
 * 클라이언트 컴포넌트가 되고 페치를 못 한다(편차 1).
 */
export function CartView({
  cart,
  addresses,
  onPlaced,
  onChanged,
}: {
  cart: CartDto;
  addresses: AddressDto[];
  onPlaced: (result: PlaceOrderResultDto) => void;
  onChanged?: () => void;
}) {
  const [addressId, setAddressId] = useState<string | null>(
    addresses.find((address) => address.isDefault)?.id ?? addresses[0]?.id ?? null,
  );

  if (cart.lines.length === 0) {
    return (
      <>
        <h1>장바구니</h1>
        <p>장바구니가 비어 있습니다.</p>
      </>
    );
  }

  return (
    <>
      <h1>장바구니</h1>
      {cart.unavailableSkuIds.length === 0 ? null : (
        <p role="alert">판매가 중지된 상품 {cart.unavailableSkuIds.length}개는 주문에서 제외됩니다.</p>
      )}
      <table>
        <thead>
          <tr>
            <th>상품</th>
            <th>단가</th>
            <th>수량</th>
            <th>소계</th>
            <th>{''}</th>
          </tr>
        </thead>
        <tbody>
          {cart.lines.map((line) => (
            <CartLineRow key={line.skuId} line={line} action={<RemoveFromCartButton skuId={line.skuId} onRemoved={onChanged} />} />
          ))}
        </tbody>
      </table>
      <p>총 {formatMoney(cart.total)}</p>

      <h2>배송지</h2>
      <AddressPicker
        addresses={addresses}
        selectedId={addressId}
        onSelect={setAddressId}
        onAdded={onChanged}
      />

      <PlaceOrderButton addressId={addressId} onPlaced={onPlaced} />
    </>
  );
}
```

`RemoveFromCartButton`은 태스크 7의 `features/cart-items` 슬라이스에 있다.

```tsx
// views/order-detail/ui/OrderDetailView.tsx
'use client';

import type { CancelOrderResultDto, OrderDto } from '@commerce/contracts';
import { isOrderCancellable } from '@/entities/order';
import { CancelOrderButton } from '@/features/cancel-order';
import { OrderSummary } from '@/widgets/order-summary';

export function OrderDetailView({
  order,
  onCancelled,
}: {
  order: OrderDto;
  onCancelled: (result: CancelOrderResultDto) => void;
}) {
  return (
    <>
      {order.status === 'PAYMENT_FAILED' ? (
        <p role="alert">결제가 거절되었습니다.</p>
      ) : null}
      <OrderSummary
        order={order}
        action={
          isOrderCancellable(order.status) ? (
            <CancelOrderButton orderId={order.id} onCancelled={onCancelled} />
          ) : null
        }
      />
    </>
  );
}
```

**`'결제가 거절되었습니다'`는 스펙 §9.10의 E2E 예시가 찾는 정확한 문구다.** 바꾸면 그 시나리오가 깨진다.

- [ ] **Step 2: view spec을 쓴다 (test-after)**

- `ProductListView.spec.tsx`: 상품이 보인다 / 빈 배열이면 안내
- `ProductDetailView.spec.tsx`: SKU마다 담기 버튼이 있다 / 가격이 SKU별로 다르게 보인다
- `CartView.spec.tsx`: 라인·총액이 보인다 / 비어 있으면 안내만 / `unavailableSkuIds`가 있으면 경고 / 기본 배송지가 처음부터 선택돼 있다 / 배송지가 없으면 주문 버튼이 `disabled`
- `OrderDetailView.spec.tsx`: `PAID`면 취소 버튼이 보인다 / `REFUNDED`면 안 보인다 / `PAYMENT_FAILED`면 거절 문구가 보인다

- [ ] **Step 3: `app/` 라우트를 배선한다**

각 페이지는 **페치와 전달만** 한다. 로직이 3줄을 넘으면 아래 레이어로 내려야 한다는 신호다.

```tsx
// app/page.tsx
import { apiBaseUrl } from '@/server/api-client';
import { createContractClient } from '@/shared/api/contract-client';
import { ProductListView } from '@/views/product-list';

/**
 * 상품 목록은 인증이 필요 없다 — 계획 3의 `ProductController`가 조회에 가드를 걸지
 * 않았다. 그래서 `createApiClient`(인증)가 아니라 `createContractClient`(비인증)를
 * 쓴다. 전자는 세션이 없으면 `SessionExpiredError`를 던지므로, 로그인하지 않은
 * 방문자가 상품 목록조차 볼 수 없게 된다.
 */
export default async function HomePage() {
  const client = createContractClient(apiBaseUrl());
  const result = await client.product.search({ query: { limit: 20, offset: 0 } });
  if (result.status !== 200) {
    return <p role="alert">상품을 불러오지 못했습니다.</p>;
  }
  return <ProductListView products={result.body.products} />;
}
```

**`createContractClient`를 `@/shared/api/contract-client`에서 import한다** — `app/`은 FSD 밖이므로 `shared`를 직접 써도 규칙에 걸리지 않는다.

```tsx
// app/products/[productId]/page.tsx — 서버 컴포넌트다('use client'가 없다)
export default async function ProductDetailPage({
  params,
}: {
  params: Promise<{ productId: string }>;
}) {
  const { productId } = await params;
  const client = createContractClient(apiBaseUrl());
  const result = await client.product.get({ params: { productId } });
  if (result.status !== 200) {
    notFound();
  }
  return <ProductDetailClient product={result.body} />;
}
```

**`ProductDetailClient`가 필요한 이유**: `ProductDetailView`의 `onAdded`가 `router.refresh()`를 불러야 하는데 서버 컴포넌트는 함수를 내려보낼 수 없다. `app/products/[productId]/product-detail-client.tsx`에 `'use client'` 래퍼를 둔다.

```tsx
'use client';
import type { ProductDto } from '@commerce/contracts';
import { useRouter } from 'next/navigation';
import { ProductDetailView } from '@/views/product-detail';

export function ProductDetailClient({ product }: { product: ProductDto }) {
  const router = useRouter();
  return <ProductDetailView product={product} onAdded={() => router.refresh()} />;
}
```

같은 형태로 `app/cart/page.tsx`(장바구니 + 주소록 두 번 페치, `CartClient`가 `onPlaced`에서 `/orders/{id}`로 이동)와 `app/orders/[orderId]/page.tsx`(`OrderDetailClient`가 `onCancelled`에서 `router.refresh()`)를 만든다.

**세션이 없으면 로그인으로 보낸다.** `cart`와 `orders` 페이지는 인증이 필요하다.

```tsx
  try {
    // ... 페치
  } catch (error) {
    if (error instanceof SessionExpiredError) {
      redirect('/sign-in');
    }
    throw error;
  }
```

- [ ] **Step 4: 통과를 확인한다**

Run: `pnpm vitest run apps/web/src/views`
Expected: PASS

Run: `pnpm --filter @commerce/web build`
Expected: 성공. **RSC의 타입 오류는 빌드에서만 드러난다** — vitest는 `app/`을 보지 않는다.

- [ ] **Step 5: 이 검사가 무엇을 잡는지 증명한다**

**(a) 취소 버튼 노출 판단이 도는가**
`OrderDetailView`의 `isOrderCancellable(order.status)`를 `true`로 바꾼다.
Expected: FAIL — `'REFUNDED면 취소 버튼이 안 보인다'`가 실패한다.
되돌린다.

**(b) 기본 배송지가 선택되는가**
`CartView`의 초기값에서 `addresses.find((a) => a.isDefault)?.id ??`를 지운다.
Expected: 관측 결과를 보고서에 적는다. 픽스처의 주소가 하나뿐이고 그것이 기본 배송지이면 `addresses[0]?.id`가 같은 값을 주어 **통과한다.** 통과하면 spec에 "기본이 아닌 주소가 먼저 오는" 케이스를 더해 판별력을 만든다.
되돌린다.

**(c) 결제 거절 문구가 정확한가**
`'결제가 거절되었습니다.'`를 `'결제 실패'`로 바꾼다.
Expected: FAIL — `'PAYMENT_FAILED면 거절 문구가 보인다'`가 실패한다. **이 문구는 스펙 §9.10의 E2E 예시가 찾는 정확한 문자열이다.**
되돌린다.

- [ ] **Step 6: 전체 검증과 커밋**

```bash
git add apps/web/src/views apps/web/app
git commit -m "feat(web): 상품·장바구니·주문 화면과 라우트를 배선한다"
```

---

### Task 11: 테스트 전용 PG 시나리오 엔드포인트 (백엔드)

**Files:**
- Create: `apps/api/src/shared/infrastructure/testing/pg-scenario.controller.ts`
- Create: `apps/api/src/shared/infrastructure/testing/pg-scenario.controller.integration.spec.ts`
- Modify: `apps/api/src/modules/payment/payment.module.ts`
- Modify: `apps/api/src/app.module.spec.ts`

**Interfaces:**
- Consumes: `FakePgAdapter`(계획 4)
- Produces: `POST /testing/pg-scenario` — body `{ scenario: 'APPROVE' | 'DECLINE' | 'TIMEOUT' }`, 204

**편차 2의 구현이다.** 스펙 §9.10의 예시가 `api.fakePg.setScenario('DECLINE_NEXT')`를 쓰고, 브라우저 E2E는 HTTP로만 말한다.

**`ENABLE_TEST_ENDPOINTS === 'true'`일 때만 등록한다.** 기본값으로는 컨트롤러가 존재하지 않는 것을 테스트가 단언한다. `.env.example`에 이 변수를 **넣지 않는다** — Playwright의 `webServer`가 자기 프로세스에만 주입한다.

- [ ] **Step 1: 컨트롤러를 쓴다**

```ts
import { Body, Controller, HttpCode, Post } from '@nestjs/common';
import { z } from 'zod';
import { ZodValidationPipe } from '../http/zod-validation.pipe';
// biome-ignore lint/style/useImportType: Nest DI가 design:paramtypes 런타임 값을 요구한다.
import { FakePgAdapter } from '../../../modules/payment/adapters/out/pg/fake-pg.adapter';

const scenarioBodySchema = z
  .object({ scenario: z.enum(['APPROVE', 'DECLINE', 'TIMEOUT']) })
  .strict();

/**
 * **테스트 전용이다.** `ENABLE_TEST_ENDPOINTS === 'true'`일 때만 모듈에 등록된다 —
 * `payment.module.ts`의 조건부 `controllers` 배열이 그것을 정한다.
 *
 * 존재 이유: 브라우저 E2E가 결제 거절 보상 경로를 밟으려면 PG 시나리오를 바꿔야
 * 하는데, `FakePgAdapter.scenario`는 Nest 프로세스 안의 상태라 HTTP 밖에서
 * 건드릴 방법이 없다(스펙 §9.10이 `api.fakePg.setScenario`를 요구한다).
 *
 * 버린 대안: 매직 금액(999원이면 거절). 프로덕션 경로의 입력값에 테스트용 의미를
 * 심으면 실서비스에서 그 금액을 결제하는 고객이 거절당한다.
 *
 * **위험**: 운영 환경에 `ENABLE_TEST_ENDPOINTS=true`가 실수로 켜지면 누구나 결제
 * 시나리오를 바꿀 수 있다. 그래서 `.env.example`에 이 변수를 넣지 않는다 —
 * 값을 아는 사람만 켤 수 있게 하는 것이 아니라, **그런 변수가 있다는 사실 자체를
 * 배포 설정에서 감춘다.** 진짜 방어는 배포 파이프라인이 이 변수를 금지하는 것이고,
 * 그것은 CI가 붙는 시점의 일이다.
 */
@Controller('testing')
export class PgScenarioController {
  constructor(private readonly pg: FakePgAdapter) {}

  @Post('pg-scenario')
  @HttpCode(204)
  setScenario(
    @Body(new ZodValidationPipe(scenarioBodySchema)) body: { scenario: 'APPROVE' | 'DECLINE' | 'TIMEOUT' },
  ): void {
    this.pg.scenario = body.scenario;
  }
}
```

- [ ] **Step 2: 조건부로 등록한다**

`payment.module.ts`:

```ts
/**
 * 테스트 전용 컨트롤러는 플래그가 켜졌을 때만 존재한다. 배열 스프레드로 조건부
 * 등록하는 이유: `@Controller` 데코레이터만으로는 등록을 막을 수 없고, 모듈의
 * `controllers` 배열이 유일한 스위치다.
 */
const testControllers =
  process.env['ENABLE_TEST_ENDPOINTS'] === 'true' ? [PgScenarioController] : [];

@Module({
  controllers: [PgWebhookController, ...testControllers],
  // ...
})
```

- [ ] **Step 3: 통합 테스트를 쓴다**

```ts
describe('PG 시나리오 엔드포인트', () => {
  it('플래그가 없으면 404다 — 컨트롤러가 등록되지 않는다', async () => {
    // 이것이 이 엔드포인트의 유일한 방어선이다. 기본값이 "없음"이어야 한다.
    const response = await request(app.getHttpServer())
      .post('/testing/pg-scenario')
      .send({ scenario: 'DECLINE' });

    expect(response.status).toBe(404);
  });
});
```

**플래그가 켜진 경우는 별도 spec 파일에서 확인한다.** `payment.module.ts`의 `testControllers`가 **모듈 import 시점에 평가되므로** 같은 파일에서 두 상태를 보려면 모듈 캐시를 비워야 하고(`vi.resetModules()`), 그러면 `AppModule`과 그 아래 전부가 다시 평가되어 실패 진단이 어려워진다.

두 파일로 나눈다:
- `pg-scenario.controller.integration.spec.ts` — 플래그 없음(기본). 404를 확인한다
- `pg-scenario.enabled.integration.spec.ts` — 파일 맨 위(**모든 import보다 먼저**)에서 `process.env['ENABLE_TEST_ENDPOINTS'] = 'true'`를 설정한다

```ts
// pg-scenario.enabled.integration.spec.ts
// 이 줄이 import보다 먼저여야 한다 — payment.module.ts가 import 시점에 이 값을 읽는다.
process.env['ENABLE_TEST_ENDPOINTS'] = 'true';

import type { INestApplication } from '@nestjs/common';
// ... 나머지 import
```

**vitest가 ESM에서 import를 호이스팅하므로 위 배치가 동작하지 않을 수 있다.** 그 경우 `vitest.config.ts`의 `api-integration` 프로젝트에 `env: { ENABLE_TEST_ENDPOINTS: 'true' }`를 주는 대신 — 그러면 기본값 테스트가 깨진다 — **`setupFiles`를 파일별로 줄 수 없으므로** 이 spec을 없애고 **태스크 12의 Playwright 스모크 테스트(`'PG 시나리오를 바꿀 수 있다'`)가 켜진 경로를 덮게 한다.** 어느 쪽이 됐는지 보고서에 적는다 — 켜진 경로에 자동 검증이 아예 없는 상태로 두지는 않는다.

켜진 경우 확인할 것:
- `{ scenario: 'DECLINE' }` → 204이고 `app.get(FakePgAdapter).scenario === 'DECLINE'`
- 열거값 밖이면 400 `VALIDATION_FAILED`
- 인증 없이 부를 수 있다(가드가 없다 — E2E가 토큰 없이 부른다)

`app.module.spec.ts`에 한 줄 더한다: 기본 환경에서 `moduleRef.get(PgScenarioController)`가 던진다.

- [ ] **Step 4: 이 검사가 무엇을 잡는지 증명한다**

**(a) 플래그 가드가 실제로 있는가**
`payment.module.ts`의 `controllers`를 `[PgWebhookController, PgScenarioController]`로 무조건 등록한다.
Expected: FAIL — `'플래그가 없으면 404다'`가 204를 받아 실패한다. **이 회귀는 운영에서 누구나 결제 시나리오를 바꿀 수 있게 만든다.**
되돌린다.

**(b) 시나리오가 실제로 적용되는가**
`setScenario`의 `this.pg.scenario = body.scenario;`를 지운다.
Expected: FAIL — 플래그를 켠 spec의 `'scenario가 바뀐다'`가 실패한다.
되돌린다.

- [ ] **Step 5: 전체 검증과 커밋**

Run: `pnpm verify`
Expected: exit 0.

```bash
git add apps/api/src
git commit -m "feat(api): E2E용 PG 시나리오 엔드포인트를 플래그 뒤에 둔다"
```

---

### Task 12: Playwright 설치와 픽스처

**Files:**
- Modify: `apps/web/package.json` (`@playwright/test@^1.56.0`, `test:e2e` 스크립트)
- Modify: `package.json` (루트 `test:e2e` 위임)
- Create: `apps/web/playwright.config.ts`
- Create: `apps/web/e2e/fixtures.ts`
- Create: `apps/web/e2e/smoke.spec.ts`
- Modify: `.gitignore`
- Modify: `apps/web/.env.example` (없으면 생성)

**Interfaces:**
- Produces:
  - `test` — `api` 픽스처가 붙은 Playwright `test`
  - `api.signUp(): Promise<{ email: string; password: string; token: string }>`
  - `api.registerCatalog(token, { onHand }): Promise<{ productId: string; skuId: string }>`
  - `api.setPgScenario(scenario): Promise<void>`
  - `api.stockOf(token, skuId): Promise<{ onHand: number; reserved: number; available: number }>`
  - `signIn(page, credentials): Promise<void>` — UI로 로그인

**`api` 픽스처가 하는 일은 시드와 관측뿐이다.** 사용자 흐름은 전부 브라우저로 밟는다 — 그것이 이 스위트의 존재 이유다. 상품·재고 등록은 관리자 화면이 없으므로(범위 절) API로 하고, 재고 확인은 화면에 안 보이므로 API로 읽는다.

**각 시나리오가 자기 사용자와 자기 상품을 만든다.** 계획 4의 사가 하니스와 같은 방식이다 — DB를 비우지 않으므로 테스트끼리 간섭하지 않고 병렬로 돌 수 있다.

- [ ] **Step 1: 설치와 설정**

```bash
pnpm --filter @commerce/web add -D @playwright/test@^1.56.0
pnpm --filter @commerce/web exec playwright install --with-deps chromium
```

`apps/web/playwright.config.ts`:

```ts
import { defineConfig, devices } from '@playwright/test';

const WEB_PORT = 3100;
const API_PORT = 3101;

/**
 * 두 서버를 Playwright가 띄운다. 포트를 개발용(3000/3001)과 다르게 두는 이유:
 * 개발 서버를 켜둔 채로 E2E를 돌릴 수 있어야 한다.
 *
 * **DB는 띄우지 않는다.** `pnpm db:up`이 먼저 돌아 있어야 하고, 그 전제를 README에
 * 적는다. Playwright가 docker를 관리하면 실패 진단이 두 겹이 된다.
 *
 * `ENABLE_TEST_ENDPOINTS`는 여기서만 켠다 — `.env.example`에 없는 이유가 그것이다(편차 2).
 */
export default defineConfig({
  testDir: './e2e',
  fullyParallel: true,
  forbidOnly: !!process.env['CI'],
  retries: 0,
  reporter: [['list']],
  use: {
    baseURL: `http://localhost:${WEB_PORT}`,
    trace: 'on-first-retry',
  },
  projects: [{ name: 'chromium', use: { ...devices['Desktop Chrome'] } }],
  webServer: [
    {
      command: 'pnpm --filter @commerce/api dev',
      port: API_PORT,
      reuseExistingServer: !process.env['CI'],
      env: {
        PORT: String(API_PORT),
        ENABLE_TEST_ENDPOINTS: 'true',
        // 스케줄러를 켠다 — TTL 자가치유가 도는 환경이어야 사가가 진짜다.
        // 다만 어떤 시나리오도 15분을 기다리지 않는다.
        SCHEDULERS_ENABLED: 'true',
      },
      timeout: 120_000,
    },
    {
      command: `pnpm --filter @commerce/web dev -- -p ${WEB_PORT}`,
      port: WEB_PORT,
      reuseExistingServer: !process.env['CI'],
      env: {
        API_BASE_URL: `http://localhost:${API_PORT}`,
        SESSION_PASSWORD: 'e2e-only-session-password-32-characters',
      },
      timeout: 120_000,
    },
  ],
});
```

`apps/api/src/main.ts`는 이미 `process.env.PORT ?? 3001`을 쓴다(계획 1). `webServer.env`의 `PORT`가 그대로 먹으므로 백엔드는 고칠 것이 없다.

**`dev` 스크립트는 `-r dotenv/config`로 `apps/api/.env`를 읽는다.** 그 파일의 `SCHEDULERS_ENABLED=false`(계획 3)가 `webServer.env`보다 **나중에** 적용되면 스케줄러가 꺼진다. `dotenv/config`는 이미 있는 환경변수를 덮어쓰지 않으므로 `webServer.env`가 이긴다 — 그래도 첫 실행에서 스케줄러 로그(`30000ms 주기로 등록했습니다`)가 뜨는지 확인하고, 안 뜨면 `dotenv_config_override`를 의심한다.

`package.json`(루트)에 더한다:

```json
"test:e2e": "pnpm --filter @commerce/web test:e2e"
```

`apps/web/package.json`에:

```json
"test:e2e": "playwright test"
```

`.gitignore`에 `playwright-report/`, `test-results/`, `apps/web/.env.local`을 더한다.

- [ ] **Step 2: 픽스처를 쓴다**

```ts
import { type APIRequestContext, expect, test as base } from '@playwright/test';

const API = process.env['E2E_API_BASE_URL'] ?? 'http://localhost:3101';

let sequence = 0;

/** 시나리오마다 다른 값을 쓴다 — DB를 비우지 않으므로 충돌을 피해야 한다. */
function uniqueSuffix(): string {
  sequence += 1;
  return `${Date.now().toString(36)}${sequence.toString(36)}`;
}

export interface SeedApi {
  signUp(): Promise<{ email: string; password: string; token: string }>;
  registerCatalog(token: string, options: { onHand: number }): Promise<{ productId: string; skuId: string }>;
  setPgScenario(scenario: 'APPROVE' | 'DECLINE' | 'TIMEOUT'): Promise<void>;
  stockOf(token: string, skuId: string): Promise<{ onHand: number; reserved: number; available: number }>;
}

function makeSeedApi(request: APIRequestContext): SeedApi {
  const auth = (token: string) => ({ Authorization: `Bearer ${token}` });

  return {
    async signUp() {
      const email = `e2e-${uniqueSuffix()}@example.com`;
      const password = 'correct horse battery staple';
      const response = await request.post(`${API}/auth/sign-up`, { data: { email, password } });
      expect(response.status(), await response.text()).toBe(201);
      return { email, password, token: (await response.json()).accessToken };
    },

    async registerCatalog(token, options) {
      const product = await request.post(`${API}/products`, {
        headers: auth(token),
        data: {
          name: `티셔츠-${uniqueSuffix()}`,
          skus: [{ code: 'RED-M', price: { amount: '12000', currency: 'KRW' } }],
        },
      });
      expect(product.status(), await product.text()).toBe(201);
      const body = await product.json();
      const skuId = body.skus[0].id as string;

      const stock = await request.post(`${API}/stock`, {
        headers: auth(token),
        data: { skuId, onHand: options.onHand },
      });
      expect(stock.status(), await stock.text()).toBe(201);

      return { productId: body.id as string, skuId };
    },

    async setPgScenario(scenario) {
      // 편차 2의 테스트 전용 엔드포인트. playwright.config.ts가 플래그를 켠다.
      const response = await request.post(`${API}/testing/pg-scenario`, { data: { scenario } });
      expect(response.status(), await response.text()).toBe(204);
    },

    async stockOf(token, skuId) {
      const response = await request.get(`${API}/stock/${skuId}`, { headers: auth(token) });
      expect(response.status(), await response.text()).toBe(200);
      return response.json();
    },
  };
}

export const test = base.extend<{ api: SeedApi }>({
  api: async ({ request }, use) => {
    await use(makeSeedApi(request));
  },
});

export { expect } from '@playwright/test';

/** UI로 로그인한다 — 쿠키를 직접 심지 않는다. 로그인 흐름 자체가 검증 대상이다. */
export async function signIn(
  page: import('@playwright/test').Page,
  credentials: { email: string; password: string },
): Promise<void> {
  await page.goto('/sign-in');
  await page.getByLabel('이메일').fill(credentials.email);
  await page.getByLabel('비밀번호').fill(credentials.password);
  await page.getByRole('button', { name: '로그인' }).click();
  await expect(page.getByRole('button', { name: '로그아웃' })).toBeVisible();
}
```

**`setPgScenario`가 전역 상태를 바꾼다.** `FakePgAdapter`는 프로세스 하나에 인스턴스 하나이므로, 병렬로 도는 두 시나리오가 서로의 설정을 덮어쓴다. **결제 시나리오를 바꾸는 테스트는 `test.describe.serial`로 묶고 끝날 때 `APPROVE`로 되돌린다.** 이 제약을 픽스처 주석과 태스크 14에 적는다.

- [ ] **Step 3: 스모크 테스트로 배선을 확인한다**

```ts
import { expect, test } from './fixtures';

test('두 서버가 뜨고 상품 목록이 열린다', async ({ page }) => {
  await page.goto('/');
  await expect(page.getByRole('heading', { name: '상품' })).toBeVisible();
});

test('시드 API가 동작한다', async ({ api }) => {
  const { token } = await api.signUp();
  const { skuId } = await api.registerCatalog(token, { onHand: 5 });

  expect(await api.stockOf(token, skuId)).toMatchObject({ onHand: 5, reserved: 0 });
});

test('PG 시나리오를 바꿀 수 있다', async ({ api }) => {
  // 편차 2의 엔드포인트가 실제로 켜져 있는지 확인한다.
  await api.setPgScenario('DECLINE');
  await api.setPgScenario('APPROVE');
});
```

- [ ] **Step 4: 실행한다**

```bash
pnpm db:up
pnpm test:e2e
```
Expected: 3개 통과.

**여기서 막히면 멈추고 원인을 적는다.** 흔한 것 셋: (1) `apps/api`의 `dev`가 `PORT`를 안 읽음, (2) `SESSION_PASSWORD`가 32자 미만, (3) `ENABLE_TEST_ENDPOINTS`가 `webServer.env`로 전달되지 않음(`reuseExistingServer`가 이미 뜬 서버를 재사용하면 플래그가 없는 프로세스를 쓴다 — 개발 서버를 끄고 다시 돌린다).

- [ ] **Step 5: 이 검사가 무엇을 잡는지 증명한다**

**(a) 플래그가 실제로 전달되는가**
`playwright.config.ts`의 `ENABLE_TEST_ENDPOINTS: 'true'`를 지운다.
Expected: FAIL — `'PG 시나리오를 바꿀 수 있다'`가 404를 받아 실패한다.
되돌린다.

**(b) 시드가 실제로 재고를 만드는가**
`registerCatalog`의 `POST /stock` 호출을 지운다.
Expected: FAIL — `'시드 API가 동작한다'`가 404(재고 없음)로 실패한다.
되돌린다.

- [ ] **Step 6: 커밋**

Run: `pnpm verify`
Expected: exit 0. **E2E는 `verify`에 없다**(편차 3) — `pnpm test:e2e`를 따로 돌린 결과를 보고서에 적는다.

```bash
git add apps/web package.json .gitignore
git commit -m "test(e2e): Playwright와 시드 픽스처를 세운다"
```

---

### Task 13: E2E — 인증·상품 조회·장바구니 (시나리오 3)

**Files:**
- Create: `apps/web/e2e/auth.spec.ts`
- Create: `apps/web/e2e/browse-and-cart.spec.ts`
- Delete: `apps/web/e2e/smoke.spec.ts` (태스크 12의 배선 확인용이었고 이제 이 셋이 덮는다)

**스펙 §9.10의 일곱 영역 중 셋이다:** 회원가입/로그인, 상품 조회, 장바구니.

테스트는 여섯 개인데, 그중 둘(잘못된 비밀번호, 미인증 리다이렉트)은 이미 열어둔 화면의 부정 경로다 — 새 픽스처도 새 화면도 필요 없으므로 §9.10이 경계한 확장이 아니다.

**이 셋은 PG 시나리오를 건드리지 않으므로 병렬로 돈다.** 각자 자기 사용자와 자기 상품을 만든다.

- [ ] **Step 1: 인증 시나리오**

```ts
import { expect, signIn, test } from './fixtures';

test('가입한 사용자가 로그인하고 로그아웃한다', async ({ page, api }) => {
  const { email, password } = await api.signUp();

  await signIn(page, { email, password });

  // 로그인하면 헤더가 바뀐다 — 세션 쿠키가 실제로 심겼다는 증거다.
  await expect(page.getByRole('button', { name: '로그아웃' })).toBeVisible();
  await expect(page.getByRole('link', { name: '로그인' })).toHaveCount(0);

  await page.getByRole('button', { name: '로그아웃' }).click();

  await expect(page.getByRole('link', { name: '로그인' })).toBeVisible();
});

test('잘못된 비밀번호로는 로그인할 수 없다', async ({ page, api }) => {
  const { email } = await api.signUp();

  await page.goto('/sign-in');
  await page.getByLabel('이메일').fill(email);
  await page.getByLabel('비밀번호').fill('wrong password entirely');
  await page.getByRole('button', { name: '로그인' }).click();

  // 스펙 §8.6: 프론트가 코드로 분기해 우리 문구를 보여준다.
  await expect(page.getByRole('alert')).toHaveText('이메일 또는 비밀번호가 올바르지 않습니다.');
  await expect(page.getByRole('button', { name: '로그아웃' })).toHaveCount(0);
});

test('로그인하지 않으면 장바구니가 로그인 화면으로 보낸다', async ({ page }) => {
  await page.goto('/cart');

  await expect(page).toHaveURL(/\/sign-in$/);
});
```

- [ ] **Step 2: 상품 조회와 장바구니 시나리오**

```ts
import { expect, signIn, test } from './fixtures';

test('상품 목록에서 상세로 들어간다', async ({ page, api }) => {
  const { token } = await api.signUp();
  await api.registerCatalog(token, { onHand: 10 });

  await page.goto('/');

  // 다른 테스트가 만든 상품도 목록에 있으므로 첫 카드가 아니라 링크의 존재를 본다.
  const firstProduct = page.getByRole('link', { name: /^티셔츠-/ }).first();
  await expect(firstProduct).toBeVisible();
  await firstProduct.click();

  await expect(page.getByRole('heading', { name: /^티셔츠-/ })).toBeVisible();
  await expect(page.getByText('12,000원')).toBeVisible();
});

test('상품을 장바구니에 담고 확인한다', async ({ page, api }) => {
  const { email, password, token } = await api.signUp();
  const { productId } = await api.registerCatalog(token, { onHand: 10 });
  await signIn(page, { email, password });

  await page.goto(`/products/${productId}`);
  await page.getByLabel('수량').fill('2');
  await page.getByRole('button', { name: '장바구니에 담기' }).click();

  await page.goto('/cart');

  await expect(page.getByRole('cell', { name: /RED-M/ })).toBeVisible();
  await expect(page.getByRole('cell', { name: '2개' })).toBeVisible();
  // 총액은 서버가 계산해 내려준다 — BFF도 프론트도 계산하지 않는다(스펙 §8.1).
  await expect(page.getByText('총 24,000원')).toBeVisible();
});

test('장바구니에서 상품을 뺀다', async ({ page, api }) => {
  const { email, password, token } = await api.signUp();
  const { productId } = await api.registerCatalog(token, { onHand: 10 });
  await signIn(page, { email, password });

  await page.goto(`/products/${productId}`);
  await page.getByRole('button', { name: '장바구니에 담기' }).click();
  await page.goto('/cart');
  await expect(page.getByRole('cell', { name: /RED-M/ })).toBeVisible();

  await page.getByRole('button', { name: '빼기' }).click();

  await expect(page.getByText('장바구니가 비어 있습니다.')).toBeVisible();
});
```

**`RemoveFromCartButton`의 라벨이 `'빼기'`여야 한다** — 태스크 7에서 그 문자열을 쓴다.

- [ ] **Step 3: 실행한다**

```bash
pnpm db:up
pnpm test:e2e
```
Expected: 6개 통과.

**불안정하면 그 자체가 발견이다.** 흔한 원인 둘: (1) `router.refresh()` 뒤 화면 갱신을 기다리지 않음 — Playwright의 자동 대기가 `expect`에만 붙으므로 클릭 뒤 `await expect(...)`로 상태를 확인해야 한다. (2) 상품 목록에 다른 테스트의 상품이 섞임 — 위 테스트가 `/^티셔츠-/`로 자기 것을 찾는 이유다.

- [ ] **Step 4: 이 검사가 무엇을 잡는지 증명한다**

**(a) 세션 쿠키가 실제로 심기는가**
`apps/web/src/server/session.ts`의 `sessionOptions`에서 `httpOnly: true`를 지운다... 는 관측되지 않는다. 대신 `app/api/auth/sign-in/route.ts`가 `signInAction`의 결과를 무시하고 항상 200을 돌려주게 바꾼다.
Expected: **통과할 수도 있다** — 쿠키는 `signInAction` 안에서 심기고 라우트의 상태 코드와 무관하기 때문이다. 관측 결과를 적고, 대신 `signInAction`의 `store.write(...)`를 지워 확인한다.
Expected(두 번째): FAIL — `'가입한 사용자가 로그인하고 로그아웃한다'`가 로그아웃 버튼을 못 찾아 실패한다.
되돌린다.

**(b) 총액이 서버에서 오는가**
`CartView`의 `formatMoney(cart.total)`을 라인 소계의 합을 프론트에서 계산하도록 바꾼다.
Expected: **통과한다** — 값이 같기 때문이다. 관측 결과를 보고서에 적고, **스펙 §8.1의 "BFF는 계산하지 않는다"를 지키는 장치가 테스트가 아니라 코드 리뷰라는 사실**을 기록한다. 되돌린다.

**(c) 인증 리다이렉트가 있는가**
`app/cart/page.tsx`의 `redirect('/sign-in')`을 지운다.
Expected: FAIL — `'로그인하지 않으면 장바구니가 로그인 화면으로 보낸다'`가 실패한다.
되돌린다.

- [ ] **Step 5: 커밋**

```bash
git rm apps/web/e2e/smoke.spec.ts
git add apps/web/e2e
git commit -m "test(e2e): 인증·상품 조회·장바구니 시나리오를 추가한다"
```

---

### Task 14: E2E — 주문 성공·결제 거절·취소 환불·재고 부족 (시나리오 4)

**Files:**
- Create: `apps/web/e2e/checkout.spec.ts`

**스펙 §9.10의 나머지 네 영역이고, §13의 기능 성공 기준이 브라우저에서 참이 되는 자리다.**

**이 파일은 `test.describe.serial`이다.** `FakePgAdapter`는 프로세스 하나에 인스턴스 하나이므로 병렬로 돌면 시나리오끼리 서로의 PG 설정을 덮어쓴다(태스크 12의 픽스처 주석). 각 테스트가 시작할 때 자기 시나리오를 설정하고, 파일 끝에서 `APPROVE`로 되돌린다.

- [ ] **Step 1: 공용 준비를 쓴다**

```ts
import { expect, signIn, test } from './fixtures';

/** 한 시나리오의 준비: 사용자·상품·재고·로그인·장바구니·배송지. */
async function readyToOrder(
  page: import('@playwright/test').Page,
  api: import('./fixtures').SeedApi,
  options: { onHand: number; quantity: number },
) {
  const { email, password, token } = await api.signUp();
  const { productId, skuId } = await api.registerCatalog(token, { onHand: options.onHand });
  await signIn(page, { email, password });

  await page.goto(`/products/${productId}`);
  await page.getByLabel('수량').fill(String(options.quantity));
  await page.getByRole('button', { name: '장바구니에 담기' }).click();
  await expect(page.getByRole('alert')).toHaveCount(0);

  await page.goto('/cart');
  // 배송지가 없으므로 추가 폼이 보인다 — 그것이 첫 주문의 실제 경험이다.
  await page.getByLabel('받는 사람').fill('홍길동');
  await page.getByLabel('연락처').fill('010-1234-5678');
  await page.getByLabel('우편번호').fill('06236');
  await page.getByLabel('주소').fill('서울시 강남구 테헤란로 1');
  await page.getByRole('button', { name: '배송지 추가' }).click();

  return { token, skuId };
}
```

**`AddressPicker`의 라벨이 위와 정확히 같아야 한다** — 태스크 7에서 `받는 사람`·`연락처`·`우편번호`·`주소`·`배송지 추가`를 쓴다. `label` 필드는 폼에서 받지 않고 `'기본 배송지'`로 고정한다(계약이 요구하는 필드이고 사용자에게 물을 가치가 없다).

- [ ] **Step 2: 네 시나리오를 쓴다**

```ts
test.describe.serial('결제', () => {
  test.afterAll(async ({ api }) => {
    // 전역 상태를 되돌린다 — 다음 파일이 승인 경로를 기대한다.
    await api.setPgScenario('APPROVE');
  });

  test('주문이 성공하면 결제 완료로 끝나고 재고가 차감된다', async ({ page, api }) => {
    await api.setPgScenario('APPROVE');
    const { token, skuId } = await readyToOrder(page, api, { onHand: 10, quantity: 2 });

    await page.getByRole('button', { name: '주문하기' }).click();

    await expect(page).toHaveURL(/\/orders\/[0-9a-f-]+$/);
    await expect(page.getByText('결제 완료')).toBeVisible();
    await expect(page.getByText('총 24,000원')).toBeVisible();

    // 예약 확정은 outbox 릴레이가 배달한 뒤에 일어난다 — 스케줄러가 켜져 있으므로
    // 곧 반영된다. 폴링으로 기다린다(스펙 §9.10의 `expect.poll`).
    await expect
      .poll(async () => (await api.stockOf(token, skuId)).onHand, { timeout: 15_000 })
      .toBe(8);
  });

  test('결제가 거절되면 그 사실이 보이고 재고 예약이 해제된다', async ({ page, api }) => {
    // 스펙 §9.10의 예시가 그대로 이 시나리오다.
    await api.setPgScenario('DECLINE');
    const { token, skuId } = await readyToOrder(page, api, { onHand: 10, quantity: 2 });

    await page.getByRole('button', { name: '주문하기' }).click();

    // 주문은 만들어졌다 — 4xx가 아니라 201이고 상태가 결과를 말한다(계획 4).
    await expect(page).toHaveURL(/\/orders\/[0-9a-f-]+$/);
    await expect(page.getByText('결제가 거절되었습니다.')).toBeVisible();

    // 보상: OrderPaymentFailed → Inventory 구독 → 예약 해제.
    await expect
      .poll(async () => await api.stockOf(token, skuId), { timeout: 15_000 })
      .toMatchObject({ onHand: 10, reserved: 0 });
  });

  test('결제된 주문을 취소하면 환불이 시작되고 재고가 복원된다', async ({ page, api }) => {
    await api.setPgScenario('APPROVE');
    const { token, skuId } = await readyToOrder(page, api, { onHand: 10, quantity: 2 });
    await page.getByRole('button', { name: '주문하기' }).click();
    await expect(page.getByText('결제 완료')).toBeVisible();
    await expect
      .poll(async () => (await api.stockOf(token, skuId)).onHand, { timeout: 15_000 })
      .toBe(8);

    await page.getByRole('button', { name: '주문 취소' }).click();

    // 계획 4의 편차 1: 환불이 끝날 때까지 REFUND_PENDING이다.
    await expect(page.getByText(/환불 처리 중|환불 완료/)).toBeVisible();

    // 확정으로 차감됐던 보유량이 되돌아온다 — release가 아니라 restore다.
    await expect
      .poll(async () => (await api.stockOf(token, skuId)).onHand, { timeout: 15_000 })
      .toBe(10);
  });

  test('재고가 부족하면 주문이 거절되고 그 이유가 보인다', async ({ page, api }) => {
    await api.setPgScenario('APPROVE');
    // 재고 1개인데 3개를 담는다.
    const { token, skuId } = await readyToOrder(page, api, { onHand: 1, quantity: 3 });

    await page.getByRole('button', { name: '주문하기' }).click();

    // 예약 단계 실패는 예외로 나온다(409) — 주문 페이지로 이동하지 않는다.
    await expect(page.getByRole('alert')).toHaveText('재고가 부족합니다.');
    await expect(page).toHaveURL(/\/cart$/);

    // 예약이 잡혔다가 풀렸으므로 재고는 그대로다.
    expect(await api.stockOf(token, skuId)).toMatchObject({ onHand: 1, reserved: 0 });
  });
});
```

- [ ] **Step 3: 실행한다**

```bash
pnpm db:up
pnpm test:e2e
```
Expected: 10개 통과(태스크 13의 6개 + 여기 4개).

**폴링 시간을 조정해야 할 수 있다.** 릴레이 주기는 5초(`OUTBOX_RELAY_INTERVAL_MS` 기본값)이므로 15초면 세 번 돈다. 여전히 불안정하면 주기를 줄이는 대신(`webServer.env`에 `OUTBOX_RELAY_INTERVAL_MS: '500'`) 타임아웃을 늘린다 — 주기를 줄이면 운영과 다른 조건에서 테스트하는 셈이고, 그 차이가 언젠가 진짜 문제를 가린다. **어느 쪽을 골랐는지 보고서에 적는다.**

- [ ] **Step 4: 이 검사가 무엇을 잡는지 증명한다 — 이 계획에서 가장 중요한 프루브다**

**(a) 결제 거절 문구가 화면에 도달하는가**
`OrderDetailView`의 `{order.status === 'PAYMENT_FAILED' ? ... : null}` 분기를 지운다.
Expected: FAIL — `'결제가 거절되면 그 사실이 보이고…'`가 실패한다.
되돌린다.

**(b) 보상이 실제로 도는가 — 백엔드까지 관통하는가**
`apps/api`의 `InventoryEventSubscriber`에서 `@OnEvent('ordering.OrderPaymentFailed')` 핸들러를 지운다.
Expected: FAIL — 거절 시나리오의 재고 폴링이 `reserved: 2`로 남아 타임아웃한다. **이 프루브가 브라우저 E2E와 사가 E2E가 같은 것을 잡는다는 것을 보여준다** — 그러므로 이 스위트의 값은 보상 검증이 아니라 **사용자가 그 경로를 밟을 수 있는가**에 있다. 그 사실을 보고서에 적는다.
되돌린다.

**(c) 재고 부족이 장바구니에 머무는가**
`usePlaceOrder`가 실패해도 `onPlaced`를 부르게 바꾼다.
Expected: FAIL — `'재고가 부족하면…'`이 `/cart`가 아닌 URL을 받아 실패한다. **이 회귀는 `undefined` orderId로 주문 상세로 이동시킨다.**
되돌린다.

**(d) 복원이 해제가 아닌가**
`InventoryEventSubscriber.onOrderCancelled`를 `wasPaid`와 무관하게 항상 `releaseForOrder`를 부르게 바꾼다.
Expected: FAIL — 취소 시나리오의 재고 폴링이 8에 머물러 타임아웃한다. **확정된 예약에 `release`를 시도하면 `ReservationConflictError`가 나고 재고가 영영 돌아오지 않는다.**
되돌린다.

- [ ] **Step 5: 커밋**

```bash
git add apps/web/e2e
git commit -m "test(e2e): 주문 성공·결제 거절·취소 환불·재고 부족 시나리오를 추가한다"
```

---

### Task 15: 마무리 — 완료 기준 점검, 문서, 이월 정리

**Files:**
- Modify: `README.md`
- Modify: `apps/api/src/modules/inventory/inventory.module.ts` (계획 4의 이월 — 불필요한 biome 억제 주석)
- Modify: `docs/superpowers/plans/2026-09-03-web-shop-e2e.md` (부록)

- [ ] **Step 1: 완료 기준을 하나씩 실제로 확인한다**

명령으로 확인하고 결과를 보고서에 적는다. **"통과했을 것"이 아니라 관측한 것만 적는다.**

**기능 (스펙 §13)**

| 기준 | 확인 방법 |
|---|---|
| 회원가입 → 로그인 → 상품 조회 → 장바구니 → 주문 → 결제 성공이 E2E로 통과 | `auth.spec.ts` + `browse-and-cart.spec.ts` + `checkout.spec.ts`의 첫 시나리오 |
| 결제 거절 시 재고 예약 해제 + `PAYMENT_FAILED` | `checkout.spec.ts`의 두 번째 |
| `PAID` 취소 시 환불 + 재고 복원 | `checkout.spec.ts`의 세 번째 |
| 주소록이 동작하고 주문에 주소가 스냅샷으로 남는다 | `checkout.spec.ts`의 `readyToOrder`가 배송지를 만들고, 주문 상세가 그 주소를 보여준다 |

**아키텍처**

```bash
pnpm arch:check
```

FSD 규칙 다섯(`fsd-shared-is-a-leaf`, 세 개의 layer-direction, `fsd-no-cross-slice-internals`)과 `no-server-code-in-fsd`가 통과하는지 확인한다. **위반을 일부러 만들어 규칙이 실제로 잡는지도 본다** — `src/entities/product/ui/ProductCard.tsx`에 `import { useAddToCart } from '@/features/cart-items';`를 넣고 `pnpm arch:check`가 실패하는지 확인한 뒤 되돌린다. 이것이 스펙 §13의 "규칙을 일부러 어기면 실패함을 확인"이다.

**테스트**

```bash
pnpm test:coverage
pnpm test:e2e
```

프론트에는 커버리지 임계값을 걸지 않는다(`vitest.config.ts`의 `thresholds`에 `apps/web`이 없다). **그 사실을 확인하고 보고서에 적는다** — 스펙 §9.11이 "경로별로 건다"고 했고 프론트에는 불변식이 없기 때문이다. UI 커버리지 목표는 렌더링을 확인하는 무의미한 테스트를 양산한다.

**산출물**

```bash
pnpm arch:graph
```

graphviz(`dot`)가 있으면 `docs/architecture.svg`가 생기고, 없으면 계획 3에서와 같이 0바이트 파일이 남는다(`.gitignore`에 이미 있다). **어느 쪽인지 관측해 적는다.** 있으면 README에서 참조하고, 없으면 생성 방법만 적는다 — 없는 파일을 참조하는 README가 더 나쁘다.

- [ ] **Step 2: 계획 4의 이월 하나를 닫는다**

`apps/api/src/modules/inventory/inventory.module.ts:2`의 `// biome-ignore lint/style/useImportType: ...` 한 줄을 지운다. `SchedulerRegistry`가 값으로 쓰이므로 `useImportType`이 발화하지 않고, biome이 매 실행마다 `suppressions/unused` 경고를 낸다.

Run: `pnpm biome check apps/api/src/modules/inventory/inventory.module.ts`
Expected: 경고 0.

- [ ] **Step 3: README에 프론트엔드 절을 더한다**

"주문 사가" 절 **뒤에**, "재고 락 전략 벤치마크" 절 **앞에** 넣는다.

```markdown
## 프론트엔드

프론트에는 헥사고날을 적용하지 않고 Feature-Sliced Design을 쓴다. 프론트의 "도메인"은
서버 도메인의 그림자이고 보안상 신뢰할 수 없는 사본이라, 진짜 불변식은 서버에 있다.

레이어는 **아래로만** 의존한다.

​```
views ← widgets ← features ← entities ← shared
```

`app/`(Next 라우팅)과 `src/server/`(BFF)는 레이어 밖이다. 백엔드와 **같은
dependency-cruiser 설정 파일**이 이 방향을 강제한다.

### `app/`이 페치하고 `views`는 그리기만 한다

`no-server-code-in-fsd` 규칙이 FSD 레이어에서 `src/server/`를 import하는 것을 막는다.
그래서 데이터 페치는 `app/`의 RSC가 하고 `views`는 props를 받는 순수 컴포넌트가 된다.

​```
app/products/[id]/page.tsx   RSC. api-client로 페치하고 props로 넘긴다
  └─ views/product-detail    순수 컴포넌트 — Testing Library로 테스트된다
```

결과적으로 E2E가 덮어야 하는 것은 `app/`의 페치 3줄뿐이다.

### 조회와 변경의 경로가 다르다

| 작업 | 경로 |
|---|---|
| 조회 | RSC → `src/server/api-client` 직접 (이미 서버다) |
| 변경 | 클라이언트 → Route Handler(`app/api/*`) → Nest |
| 인증 | Route Handler (`Set-Cookie`가 필요하다) |

브라우저는 액세스 토큰을 보지 않는다. 토큰 주입과 401 시 refresh 재시도가 BFF
한 곳(`src/server/api-client.ts`)에 모인다.

### 테스트

프론트의 seam은 포트가 아니라 **네트워크**다. MSW가 그 자리이고 핸들러는
`@commerce/contracts`의 Zod 스키마로 요청과 응답을 검증한다 — 백엔드 계약이 바뀌면
프론트 목이 즉시 깨진다.

| 레이어 | 테스트 | TDD |
|---|---|---|
| `shared/lib`, `entities/*/model` | Vitest 단위 | 적용 |
| `features/*/model` (훅) | Vitest + `renderHook` + MSW | 적용 |
| `features/*/ui`, `widgets`, `views` | Testing Library + MSW | test-after |
| `app/` (RSC) | Playwright | 미적용 |

## 실행

​```bash
pnpm db:up                 # Postgres
pnpm --filter @commerce/api dev    # :3001
pnpm --filter @commerce/web dev    # :3000
```

`apps/web/.env.local`에 `SESSION_PASSWORD`(32자 이상)와 `API_BASE_URL`이 필요하다.

## E2E

​```bash
pnpm db:up
pnpm test:e2e
```

Playwright가 두 서버를 별도 포트(3100/3101)로 띄우므로 개발 서버를 켜둔 채 돌릴 수
있다. **`pnpm verify`에는 들어 있지 않다** — 두 서버와 브라우저를 띄우는 비용을
커밋마다 치르지 않기 위해서다. CI가 붙으면 별도 잡으로 넣는다.

스펙이 정한 일곱 영역을 덮는다: 회원가입/로그인, 상품 조회, 장바구니, 주문 성공,
결제 거절 보상, 주문 취소 환불, 재고 부족. 테스트는 10개이고 셋은 같은 화면의
부정 경로다(잘못된 비밀번호, 미인증 리다이렉트, 장바구니에서 빼기).
```

README의 백틱 세 개는 실제 파일에서 이스케이프 없이 쓴다 — 위 블록의 `​```는 문서 안에 코드 펜스를 중첩해 보이기 위한 표기다.

**"구조" 절의 문장을 갱신한다** — 계획 4가 "남은 것은 프론트엔드 상점 화면과 Playwright 브라우저 E2E"라고 적어 두었고, 이제 그것이 끝났다.

- [ ] **Step 4: 이월을 부록에 적는다**

계획 문서 끝에 부록으로 남긴다. 최소한 다음을 포함한다.

- **E2E가 `pnpm verify`에 없다**(편차 3) — CI가 붙는 시점에 별도 잡으로 넣는다
- **`ENABLE_TEST_ENDPOINTS`의 진짜 방어는 배포 파이프라인이다**(편차 2) — 지금은 `.env.example`에 없다는 것뿐이다
- **`FakePgAdapter`가 프로세스 전역이라 결제 시나리오 테스트가 직렬이다** — 병렬로 돌리려면 요청별 시나리오(헤더나 주문 메타)가 필요하고, 그것은 프로덕션 경로에 테스트 개념을 심는 일이다
- **주문 목록 화면과 주소록 CRUD 화면이 없다**(범위 절) — API는 있다
- **`ProductDetailView`에 옵션 선택 UI가 없다** — SKU가 코드 문자열 하나뿐이라 고를 축이 없다. 옵션 축(색·사이즈)이 생기면 만든다
- **`CartView`가 수량 변경 UI를 갖지 않는다** — `PUT /cart/items/:skuId`는 API에 있고 `features/cart-items`에 훅을 더하면 된다
- **`app/` 아래 페치 코드에 단위 테스트가 없다**(편차 1) — 3줄을 넘기 시작하면 로직이 새고 있다는 신호다
- 실행 중 발견한 것들

- [ ] **Step 5: 커밋**

Run: `pnpm verify` → exit 0
Run: `pnpm test:e2e` → 전부 통과

```bash
git add README.md docs apps/api/src
git commit -m "docs: 프론트엔드 구조와 계획 5 완료 기준 점검 결과를 남긴다"
```

---

## 완료 기준

이 계획이 끝났을 때 **스펙 §13의 다음이 참이어야 한다.**

**기능**
- 회원가입 → 로그인 → 상품 조회 → 장바구니 → 주문 → 결제 성공이 브라우저 E2E로 통과
- 결제 거절 시 재고 예약이 해제되고 주문이 `PAYMENT_FAILED`로 끝남 (브라우저 E2E)
- `PAID` 상태 주문 취소 시 환불되고 재고가 복원됨 (브라우저 E2E)
- 주소록이 동작하고 주문에는 주소가 스냅샷으로 남음

**아키텍처**
- `pnpm arch:check`가 통과하고, FSD 규칙을 일부러 어기면 실패함을 확인
- FSD 레이어가 `src/server/`를 import하지 않음
- `apps/web`이 `apps/api`를 import하지 않음

**테스트**
- 프론트 MSW 핸들러가 contracts 스키마로 검증되어, 계약 변경 시 깨짐
- `features/*/model` 훅에 TDD가 적용됨
- **E2E가 스펙 §9.10의 일곱 영역을 전부 덮는다.** 테스트 파일은 10개인데, 셋(잘못된 비밀번호, 미인증 리다이렉트, 장바구니에서 빼기)은 이미 열어둔 화면의 부정 경로라 **새 픽스처도 새 화면도 요구하지 않는다** — §9.10이 경계한 "유지비만 커지는 확장"이 아니다. 새 영역을 더하려면 그 판단을 다시 해야 한다.

**산출물**
- README에 프론트엔드 구조·실행 방법·E2E 실행 방법

**이 계획이 끝나면 스펙 §13의 모든 성공 기준이 참이 된다** — 계획 1~4가 백엔드를, 계획 5가 프론트와 E2E를 덮는다. 남는 것은 §1.3이 처음부터 범위 밖으로 둔 것들(반품, 부분 환불, 역할 기반 인가, 소셜 로그인)과 각 계획의 부록에 쌓인 이월이다.
